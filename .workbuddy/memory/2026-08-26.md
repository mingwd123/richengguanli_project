# 2026-08-26 工作日志

## 功能方向建议与 v2.x 计划文档

- 阅读项目（Dayliane 日程提醒与任务协作系统）后，给出三端功能方向建议，核心判断：项目的差异化护城河是「疲劳评估 + 负荷管理」，建议资源优先投向该主线，而非堆通用功能。
- 按建议在 `计划/` 目录生成了三份计划文档（未改动任何代码）：
  - `用户端计划v2.1.md`：预计/实际投入时间与跨天分摊、团队任务纳入负荷、周期任务、周/月疲劳回顾、AI 主动排程、iCal 订阅。
  - `桌面端计划v2.2.md`：版本化安装包与自动更新（发布门禁）、全局快捷键快速录入、托盘倒计时+专注计时、离线优先本地缓存。
  - `管理端计划v2.3.md`：运营数据看板、备份监控只读面板、AI 成本配额控制、账号风控。
- 用户明确：AI 收费「接下来再讨论」，未纳入 v2.3；AI 配额仅做成本可见与额度控制的技术准备。
- 遗留待讨论：AI 收费/定价方案。

## 桌面端深度调研与 v2.2.1 补强

- 用户对桌面端有较大期望，要求着重看桌面端现状。已通读 `src-tauri` Rust 层（lib.rs、tauri.conf.json、Cargo.toml）、`native.ts`、`desktopShell.ts`、`DesktopLayout.vue`、托盘逻辑与依赖清单。
- 现状判断：桌面端已有托盘、置顶、透明度、原生通知、快捷时间轴、关闭隐藏托盘、45 秒轮询，但缺"常驻灵魂"（开机自启、单实例、全局快捷键、窗口记忆、跟随系统主题）。
- 新增《桌面端计划v2.2.1.md》，在 v2.2 基础上补强：开机自启+单实例、窗口位置/大小记忆、跟随系统深色模式、全局搜索启动器、桌面悬浮迷你待办、任务栏角标+剪贴板记录、专注联动系统勿扰、崩溃日志收集上报（共 9 个阶段）。
- 桌面端核心差异化判断：全局搜索启动器是王牌；专注计时+勿扰联动可与疲劳评估形成闭环（专注时长回填 v2.1 的 actual_minutes）。

## 管理端视图体系与 v2.3.1 合并

- 用户希望管理端有"许多视图来观察"。已通读 admin-web views 与后端 AdminService.dashboardStats()，现状是"入口导航 + 8 个 count(*) 快照"，无趋势/分布/下钻/预警。
- 提出六大类数据视图体系：运营健康 / 协作任务 / 疲劳负荷(核心差异化) / AI 成本 / 系统运维 / 安全风控。
- 新增《管理端计划v2.3.1.md》，在 v2.3 基础上合并视图体系（7 个阶段），核心是补上管理端缺失的"全站疲劳/负荷"全局视角。
- 落地共识：Element Plus + ECharts，数据源复用现有表，只加聚合 SQL + 前端图表；Dashboard 改「运营总览」+ 侧边栏「数据视图」二级菜单。
