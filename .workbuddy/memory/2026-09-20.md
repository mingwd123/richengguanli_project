# 2026-09-20 工作日志

## 按《计划/用户端疲劳度每日进度修复.md》完成 P01–P11 修复

用户提供新修复计划文档（11 个问题、6 个阶段），要求按文档修复对应功能。已全部落地，文档 §十一「完成记录模板」表格按文档要求填写（只动该表格）。

### 后端（V28 + ScheduleService + FatigueService）

- **P01 并发**：`lockProgressSchedule` 用 `SELECT ... FOR UPDATE` 锁任务行，锁内重读 `status/progress_percent/progress_tracking_enabled` 再算增量；当天每日记录也用 `for update` 读取；提交前 `assertProgressConsistency` 校验「每日增量之和 == progress_percent」「completed 必须 100%」「进度任务不得带整项完成快照」，不一致时 `log.error` 并抛异常回滚。
- **P02 完成日期**：新增 `V28__schedule_progress_completed_date.sql`（列 + 索引 + 两步回填：先取每日记录中累计首次 ≥100 的日期，退化再用 `completed_at` 的日期）。新增 `firstCompleteProgressDate` / `syncProgressCompletion`：pending 且累计 100% 才写完成状态与进度完成日期；completed 状态下修正会同步日期但不改状态；累计不足 100% 时重新打开并清空完成字段。`FatigueService.scheduleCompletionDate` 让日汇总/报告/详情优先用进度完成日期。
- **P04 状态机**：补录/修正不会把 `cancelled` 自动改成完成（`status='pending'` 条件保证）；取消期间提交进度返回 400。团队任务 `accepted -> completed` 的锁 + 条件更新**既有实现已满足**，未改动代码。
- **P05 调查口径**：`surveyToday` / `availableDates` / `surveyPending` / `scanSurveyPrompts` 统一改用「个人完成负荷 > 0」判断（新增 `hasPersonalCompletedLoad`），部分进度也能进日终调查。
- **P06 列表负荷**：`applyProgressCompletedLoads` 批量聚合每日负荷（`in (:ids)` 一次查询，避免 N+1），进度任务在 `completedLoad(item)` 中不再回退到计划疲劳权重；`requireSchedule` 也补 `progressCompletedLoad`。
- **P08 补录下限**：`progressBackfillMinDate` = max(任务有效起始日期, 今天 − max(30, retentionDays))；新增 `FatigueService.retentionDays()`。响应新增 `completedDate/canSubmitProgress/canBackfill/backfillMinDate/backfillMaxDate/retentionDays`。
- **P10 完成率**：报告改为先构造统一任务集合（计划日在本期 ∪ 完成日在本期，排除无完成记录的取消任务），分子分母同源；分子>分母时 `log.error` 并返回 `null`（不静默截断）。

### 前端

- 新增共用组件 `src/web-user/src/components/ScheduleProgressPanel.vue`（进度条、逐日表、提交/修正/补录弹窗、状态与错误态、`defineExpose({openSubmit, openBackfill})`），网页详情页与桌面快捷详情都复用，避免两端各写一套规则。
- 网页端：`ScheduleDetailPage` 改为挂载组件并只读共享状态做头部展示；`SchedulesPage` 用 `handleComplete` 统一列表/右键菜单/状态菜单入口（进度任务跳详情页）；store 新增 `resetScheduleProgress` 与请求序列号（P11），加载失败保留错误态且清空旧数据；P03 只在「追踪开启且有新增进度」时下发 `fatigueLevel`；P09 640px 以下进度表转纵向卡片。
- 桌面端：`DesktopQuickScheduleEditor` 加每日进度开关；`DesktopQuickTimeline` 详情挂载同一面板、完成按钮改走 `openSubmit(100)`、时间轴负荷改用 `progressCompletedLoad`；`newScheduleForm`/`scheduleEditForm` 补该字段。
- 共用判断 `canTrackDailyProgress(form)` 放进 `web-user/utils/helpers.ts`，网页与桌面共用并加了单测。

### 验证

- `mvn clean test`：**213 项 0 失败**（含 MySQL 真实 28 个迁移路径）。
- web-user **65 项**、desktop **25 项**、两端 typecheck 无报错。
- 未做：真实浏览器交互与 375/390/768px 视口实测（沙箱内没有浏览器，`agent-browser` 未安装）。

### 口径决定（已写入修复文档 §十一 备注）

完成率集合**只纳入「本期完成」的跨周期任务**，不把「期初未完成且本期仍挂起」的任务计入分母——否则分母随挂起任务膨胀且随完成时间漂移，报告不稳定；这样分子恒为分母子集，完成率不会超 100%。这一点与文档 §2.4 第 1 条第二个 bullet 的字面读法略有差异，已向用户说明。

### 沿用的经验

- 沙箱启动后端必须用 `-Dspring-boot.run.arguments=--server.port=8080`，否则 `SERVER_PORT=0` 会让它随机端口（`env -u` 无效）。
- 新增表/列要同步 `schema-test.sql`（含顶部 `DROP TABLE IF EXISTS`）。
- 多列 `ALTER TABLE ... ADD COLUMN a, ADD COLUMN b` 在 MySQL 可用、H2 不可用，但测试走 `schema-test.sql`，不受影响。

---

## 安装 agent-browser 并做真实浏览器验收

用户授权「沙盒安装 agent-browser」，安装与实测都已完成。

### 安装方式（全局装会失败）

- `npm install -g agent-browser` 报 `EPERM mkdir D:\Node_24\node_global\node_modules\agent-browser`（沙箱不允许写该目录）。
- **可用做法**：装进托管工作区
  ```bash
  WS="/c/Users/ming/.workbuddy/binaries/node/workspace"
  mkdir -p "$WS" && cd "$WS" && npm install agent-browser --no-fund --no-audit
  "$WS/node_modules/.bin/agent-browser" install    # 下载 Chrome 153 到 C:\Users\ming\.agent-browser\browsers
  ```
  调用时始终用 `$WS/node_modules/.bin/agent-browser`（不在 PATH 上）。
- 首次 `open` 可能静默失败（exit 1 无输出），加 `--debug` 再试即可成功。

### 关键命令

- `open <url>` / `reload` / `close`；`snapshot -i` 拿 ref；`click eN` / `check eN` / `fill eN "text"` / `select eN value`。
- 原生 `<select>`、`<input type=datetime-local>` 在 snapshot 里常常没有名字，**用 `eval` 直接设值 + 派发 `input` 事件**最稳：
  `eval '(() => { const i=document.querySelector(".modal-panel input[type=date]"); i.value="2026-09-25"; i.dispatchEvent(new Event("input",{bubbles:true})); })()'`
- `set viewport <w> <h>`（注意是 `set viewport`，不是 `viewport`）；`screenshot <path>`（`--full-page` 不支持）。
- 检查横向溢出：`eval 'JSON.stringify({w:document.documentElement.clientWidth, o:document.documentElement.scrollWidth-document.documentElement.clientWidth})'`。
- 截图默认落在 `C:\Users\ming\.agent-browser\tmp\screenshots\`，产出交付用的图要显式传路径。

### 实测结论

- 跑通了：新建表单开关（点事件置灰、任务类型可勾）、列表「提交进度」入口、详情面板、提交 40%（负荷 2 点）、完成剩余进度（自动完成、负荷 5 点、完成日显示进度日期）、取消态只剩恢复、375/390/768/1024px 无横向溢出。
- **注意**：本次后端仍是 2026-09-13 启动的旧进程（库在 V27），所以 V28 与新字段（`canSubmitProgress`/`backfillMinDate`/`completedDate`）未在浏览器中验证；补录弹窗的日期默认值因此为空（新后端会默认今天）。

### 浏览器实测抓出并修掉的两个缺陷

1. `ScheduleProgressPanel.vue` 里「任务已取消」提示与进度记录表写在同一个 `v-if/v-else-if` 链上 → 取消态看不到逐日记录。已拆成互不依赖的条件（已验证 rows=1 且提示同时在）。
2. `.modal-panel label { display: grid }` 覆盖开关的 `display: flex` → 勾选框与文案上下堆叠。已在 `style.css` 加 `.modal-panel label.progress-toggle` 高优先级规则修正。

这正是「自动化测试全绿但界面仍是坏的」的典型例子——以后做完 UI 功能应当真机点一遍。

---

## 重启三端到 V28 + 在新后端上复验，修掉 3 个新发现的问题

用户要求「把你发现的问题修复一下」。先把 09-13 起的旧后端（库在 V27）换成新后端，再在新后端上重跑浏览器全链路。

### 环境动作（已获用户指令）

- 停掉 8080/5173/5174 → 以 `./mvnw.cmd spring-boot:run -Dspring-boot.run.arguments=--server.port=8080` 重启后端，Flyway 应用 **V28**（`schedule.progress_completed_date` 列已存在），另起 web-user(5173) 与 admin-web(5174)。
- 验证 P07 时临时起过 desktop vite(1420)，验完**已停掉**，环境恢复为「后端 + 2 前端」。

### 新后端上的浏览器复验（全部通过）

补录默认今天、上下限 `2026-09-18 ~ 2026-09-20`；提交 70% 时预览「保存后累计 70%」；提交 90%；完成剩余进度 → 自动完成、完成日 = 进度日期、无整项快照；修正首行到 0 → 自动重开、完成日清空；取消态可查可修正、修正到 100% 也**不会**自动完成；恢复后可「标记完成」；375/390/768/1024px 无横向溢出。

### 本轮修复

1. **面板布局样式整块失效**：`ScheduleProgressPanel` 是多根节点（fragment），调用方的 `class` 被 Vue 丢弃并刷 12 条警告，`.detail-progress-panel` / `.quick-detail-progress` 全部失效 → 面板 `margin-top` 恒为 0。改法：组件 `defineOptions({ inheritAttrs: false })` + `attrs.class` 显式绑到面板根节点；网页侧 scoped 规则改 `:deep()`（父组件 scope 属性不会落到 fragment 根节点）。修后 `margin-top:24px` 生效、控制台零警告。
2. **「待办 + 累计 100%」无完成入口**：取消后恢复 / 修正回退再补回都会落到这个状态，「完成剩余进度」因 `percent < 100` 被隐藏。抽出 `isProgressComplete` / `progressCompletionActionLabel` / `progressSubmitDialogTitle` 到 `utils/helpers.ts`（网页与桌面共用 + 3 个新单测），该状态显示「标记完成」、弹窗标题「标记任务完成」。
3. **并发用例断言过严**：加行锁后并发提交（50 与 100）时后到的一笔在锁内读到 `completed`，以 `schedule must be pending` 拒绝（比 `progress cannot decrease` 更准确）。测试放宽为接受两种合法业务拒绝，连跑 3 次稳定。

### 验证

- `mvn clean test` **213 项 0 失败**（含 MySQL 真实 28 迁移）；并发用例单独连跑 3 次通过。
- web-user **67 项**（+2）、desktop **25 项**，两端 typecheck 无报错。
- 桌面端：可在浏览器打开 1420 并登录，验证了「持续」模式下的进度开关与创建进度任务成功；**但浏览器模式下快捷时间轴取不到日程数据**，快捷详情的面板视觉仍需 Tauri 实机。
- 所有验证用任务（id 39/40/41/42）已删除；用户自己的任务（含 `学习高数`，9-13 的 60% + 9-20 的 40%）未做任何改动。

---

## 移除登录页的开发遗留演示账号

用户截图指出用户端登录页有「演示账号 13800138000 / Abc12345」，管理端也有。

改动（都是纯删除，没有新增 UI）：

- `web-user/src/views/LoginRegister.vue`：删掉 `<p class="hint demo-account">演示账号 …</p>`（桌面端通过 `@web` 复用同一组件，一并生效）。
- `web-user/src/style.css`：删掉随之失效的 `.demo-account` 规则。
- `web-user/src/utils/labels.ts`：删掉全项目无人使用的 `demoAccount` 文案项。
- `web-user/src/stores/app.ts`：`loginForm` 默认值 `'13800138000' / 'Abc12345'` → 空字符串（这才是用户端的「预填」来源）。
- `admin-web/src/stores/admin.js`：`loginForm` 默认值 `'admin' / 'Admin12345'` → 空字符串（管理端本来没有文字提示，暴露点就是这个预填）。

验证：两端登录页输入框均为空、页面无「演示账号」字样；手动输入凭据仍能正常登录（用户端 `/`、管理端拿到 token）；空表单提交管理端返回 401 提示而非报错。web-user 67 项、admin 44 项、desktop 25 项、两端 typecheck 全通过。

保留未动：测试账号仍记录在 `启动.md`、`测试团队账号密码.md`；桌面端标题栏的 `Preview` 是 `runningInTauri ? 'Desktop' : 'Preview'` 的有意分支，不是遗留。

---

## 增加「记住密码」，并压掉浏览器自带自动填充

用户指出登录框仍被自动填上（截图里输入框是浏览器 autofill 的淡紫色底色），要求去掉自动填写，改成用户勾选「记住密码」后才回填。

### 改动

- 新增 `web-user/src/utils/rememberedLogin.ts`、`admin-web/src/utils/rememberedLogin.js`：localStorage 读写（`dayliane_login_remembered` / `dayliane_admin_login_remembered`），base64 编码（**不是加密**，注释里写明了），读失败/字段不全/存储不可用都返回 null 或静默忽略。
- 两个 store：`loginForm` 初始值改为从记住记录回填；新增 `rememberPassword`（有记录即默认勾选）与 `syncRememberedLogin()`；登录成功才写入，未勾选则清除并清空密码框（web-user 保留原 `finally` 清空密码的行为，仅勾选时不擦）。
- 两个登录页：密码框上方加「记住密码」勾选框；`autocomplete` 从 `username` / `current-password` 改成 `off` / `new-password`，表单加 `autocomplete="off"`，压掉浏览器自带自动填充。
- 样式：两端 `.login-form label` 都是 `display: grid`，勾选框行必须显式改回 flex（`.remember-toggle` / `.admin-remember-toggle`），否则勾选框与文字上下堆叠。

### 验证

浏览器实测（agent-browser）：初始两页输入框全空、勾选框未选、无存储记录；勾选并登录 → 写入记录（base64，非明文）；清掉 token 再进 → 账号密码回填 + 勾选框选中；取消勾选 → 记录立即清空；手动输入仍能正常登录。测试：web-user **76 项**（+9，含 5 项编解码单测 + 4 项 store 用例）、admin **47 项**（+3）、desktop 25 项，两端 typecheck 无报错，控制台无错误（`errors` 命令在空白页也输出同样的空 `✗` 行，是工具噪声）。

### 顺带更新

`MEMORY.md` 补「浏览器验收」注意事项；技能 `dayliane-feature-delivery` 修正了 V27→V28、补了 fragment 根节点与 label grid 两个坑、加了浏览器验收与收尾章节。

---

## 归档「用户端疲劳度每日进度修复」计划

用户确认后把 `计划/用户端疲劳度每日进度修复.md` 移入 `计划/已完成/`。归档前只改了两处与本文件夹语义直接相关的内容：头部状态行（`已确认，待实施` → `已实施完成` + 完成日期 + Tauri 遗留说明）、§十一 表格的阶段 0 行（部分完成 → 已完成，注明基线以自动化用例固化、不归档样例文件）。

回答用户「是否完全完成」时的口径：**P01–P11 全部落地，六个阶段完成**；唯一遗留是「桌面端 Tauri 实机视觉复核」（沙箱里桌面浏览器模式取不到日程数据，只能用户在自己机器上看），已写进文档 §十一。

---

## 归档「用户端计划v2.1-2」

该计划此前只剩 1 项未勾选（视口与控制台人工复核）。本轮用 agent-browser 补上了实测：

- **教训**：第一轮 375px 巡检结果作废——脚本里误执行了 `localStorage.setItem("dayliane_token","")`，导致所有路由都被重定向到登录页，等于只测了登录页 13 遍。巡检前必须先确认登录态。
- 重新登录后实测：375px 下 12 个路由（首页/日历/列表/详情/团队/任务/通知/调查/回顾/提醒/我的/设置）横向溢出全为 0；1280px 下 6 个关键页面同样为 0；控制台无错误无警告。注意 `/profile/settings` 页自带 4 个密码框（改密码表单），用「有无密码框」判断登录态会误报。

文档只改 3 处：勾选最后一项、§7.3 第 4 条改为复核记录（附实测数据）、头部「最近补齐」补一句 2026-09-20 归档。107/107 全勾选后移入 `计划/已完成/`。`计划/` 根目录剩余 6 份：后续计划、工单模块、日程提醒App、桌面端 v2.2.1、移动端、管理端 v2.3.1。


---

## 按《工单模块开发.md》完成工单模块（6 阶段全部落地）

用户确认版本保存后要求按计划完整开发。V29 新增 8 张表（ticket_setting/ticket/ticket_message/ticket_attachment/ticket_follow/ticket_reaction/ticket_event/ticket_idempotency）。

### 后端（com.dayliane.ticket 包）
- `TicketSettingService`：单例开关（默认 FALSE），复用注册开关写法；更新写入 admin_operation_log；值未变短路。
- `TicketService`：状态机（open/in_progress/waiting_reporter/resolved/closed）、管理端乐观版本号 409、合并/取消合并（禁环禁嵌套、关注转移去重）、我也遇到/关注幂等、作者确认/撤回/重开、内部备注不通知不进公开响应、通知同事务写入（异常吞掉不影响主流程）。
- `TicketAttachmentStorage`：随机存储键、扩展名+魔数+ImageIO 解码三重校验、40MP 上限、临时→绑定、每日 3:40 清理（只删 temporary）；WebP 依赖 twelvemonkeys imageio-webp（pom 新增）。
- `TicketRateLimiter`：进程内滑动窗口（单实例前提），配置集中在 dayliane.ticket.rate-limit。
- 通知复用 notification 表 type='ticket' + target_route=/tickets/{id}；用户通知列表/未读数 SQL 过滤「模块关闭或工单隐藏」（NotificationService.USER_NOTIFICATION_VISIBILITY）。
- multipart 上限需显式配置（默认 1MB）：spring.servlet.multipart 11MB/13MB。

### 前端
- web-user：stores/tickets.ts（开关 60s 节流刷新、503=TICKET_FEATURE_DISABLED 统一处理、附件 blob URL）、TicketsPage（大厅+查重+创建弹窗+离开确认）、TicketDetailPage（原文/解决摘要/讨论分页/时间线/灯箱/作者操作）、SideNav「工单」入口按开关显隐、路由 /tickets。
- admin-web：stores/tickets.js + TicketsPage（列表筛选/详情对话框/公开回复与内部备注区分/状态变更/认领/优先级/置顶/隐藏/合并取消合并/超管开关带确认）；el-image 无法带鉴权头，用 blob URL 展示；apiUpload 处理 multipart。
- desktop：路由接入同两个页面（@web 复用）。

### 验证
- 后端 `mvn clean test` 228 项 0 失败（含真实 MySQL 29 个迁移）；TicketTests 15 项覆盖 §14 全场景。
- web-user 76 / admin 47 / desktop 25 全过，typecheck 无报错。
- 浏览器实测完整链路：用户提交 → 管理员公开回复（带管理员标记）→ 用户通知徽标 → 管理员标记已解决 → 作者确认解决 → 关闭且禁回复；隐藏工单用户 404；模块关闭后 ticket 通知从用户列表消失；375px 工单两页 0 溢出 0 控制台错误。
- 验收测试工单 TK-00000001 已由管理员隐藏；模块当前处于「开启」状态供用户试用。

### 踩坑（已入技能/记忆）
- `onBeforeRouteLeave` 来自 vue-router 不是 vue（引错导致整页白屏）。
- MySQL 驱动 UTC 会话下 DATETIME 返回 LocalDateTime，queryForList 后强转 Timestamp 会 500；要用 rs.getTimestamp 或按 LocalDateTime+UTC 处理。
- agent-browser 操作 el-select 需真实鼠标事件（synthetic click 打不开下拉）；页面 window.confirm 会阻塞 CDP，需先 `window.confirm=()=>true` 打补丁。
- 新表带 UNIQUE NOT NULL 列（ticket_no）时先插随机占位再回写，不能事后 update 补值。

---

## 注册成功改为跳转登录页并预填账号密码

用户反馈注册后以为失败（原实现是注册即自动建会话跳工作台，静默失败时无反馈）。改动（用户端 + 桌面端经 @web 自动继承）：
- `stores/app.ts` register()：成功后不再 establishSession/loadAll，提示「注册成功，已为你填好登录信息」。
- `views/LoginRegister.vue` handleRegister()：提交前留存 email/password（store 的 finally 会清密码），成功后 switchTab('login') 并回填 loginForm.account/password。

验证：typecheck + 76 项测试通过；浏览器端到端（库里造 HMAC 摘要验证码绕过真实邮件）确认「注册 → 切登录页签 → 预填 → 直接登录进工作台」全链路；登录页控制台 0 错误。测试账号 reg-e2e-0920@test.local 留在库中。

坑：EmailVerificationFields 的验证码框是受控输入（:value + @input 清洗），自动化赋值后若紧接着的 email 更新触发重渲染可能被重置——赋值后先读回确认再提交。

---

## 修复备案号随内容上下跳动

根因：`.compliance-footer` 在 `.main-area` 里跟普通内容流排布（margin-top: 30px），内容高度一变它就移动。修复（style.css）：
- `.main-area` 改纵向 flex + `min-height: 100vh`，`.main-area > .compliance-footer` 用 `margin-top: auto` 沉底。
- 移动端（≤900px，底部导航实际 66px，由后段媒体查询 `.app-frame { padding-bottom: 66px }` 决定）：`.main-area` min-height 扣 66px，覆盖必须写在文件靠后的 1663 行那段媒体查询里（早处的覆盖会被后面无条件的 min-height: 100vh 压掉）。

验证：短内容页（/reminders、/teams、/fatigue/survey）footer 贴住视口底部不再跳；长页滚动到底 footer 在文档末尾；375px 下 footer 在底部导航之上且无常驻滚动条；76 项测试通过，控制台 0 错误。
