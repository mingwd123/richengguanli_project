# 日程提醒 App 计划文档

## 1. 项目概述

### 1.1 App 名称

日程提醒 App

### 1.2 产品定位

日程提醒 App 是一款面向个人和小团队的日程规划与任务协作工具。

它既可以帮助个人安排每天的日程、避免遗忘重要事项，也可以支持团队成员之间分配任务、确认接收、拒绝任务、完成任务，并进行提醒通知。

### 1.3 目标用户

- 想对每天有清晰规划的人
- 容易忘记事情、需要提醒的人
- 学生小组、工作小组、创业团队
- 需要进行任务分工和协作的小型团队

### 1.4 解决的问题

- 个人容易忘记事情
- 每天没有清晰的计划安排
- 团队任务分配后缺少确认机制
- 创建者不知道成员是否接受任务、是否完成任务
- 团队任务和个人日程分散，不方便统一查看

## 2. 技术方案

### 2.1 用户端

第一版优先开发网页端：Vue 3

说明：

- 用户可以通过浏览器登录和使用系统
- 安卓端暂时搁置，后续再开发 Kotlin 原生 App
- 网页端和安卓端后续共用同一套 Spring Boot 后端接口

### 2.2 后端

Java Spring Boot

### 2.3 数据库

MySQL

数据库配置原则：

- 本地开发可以使用本地 MySQL 或 Docker MySQL
- 后续部署可以切换为云数据库
- 数据库连接信息不能写死在代码里
- 数据库地址、端口、库名、用户名、密码统一通过配置文件和环境变量管理
- 切换本地数据库、测试数据库、云数据库时，只修改环境配置，不修改业务代码

### 2.4 管理后台

Vue

### 2.5 AI 能力

第一版建议接入 AI 服务，为日程创建、任务拆解和计划建议提供智能辅助。

AI 能力通过后端统一封装，不在前端直接调用 AI 接口，方便后续更换模型、控制权限和记录调用日志。

AI 接入安全原则：

- 前端不直接调用 AI 服务商接口
- 前端不保存、不展示 AI API Key
- 所有 AI 请求统一经过 Spring Boot 后端
- AI API Key 存放在服务器环境变量或安全配置中，不提交到代码仓库
- 后端记录 AI 调用日志，方便管理员查看调用情况和排查问题
- 管理后台只展示 AI 服务状态、调用记录和功能开关，不展示完整密钥

第一版 AI 接入建议：

- 使用单一 AI 服务商
- 后端封装统一的 AiService
- 通过配置项控制 AI 功能是否启用
- 预留 provider/model 配置字段，方便后续切换模型

### 2.6 配置与部署管理

为了方便后续从本地数据库切换到云数据库，以及频繁更换 AI API Key，项目需要提前采用环境化配置。

后端建议使用 Spring Boot 多环境配置：

- application-dev.yml：本地开发环境
- application-test.yml：测试环境
- application-prod.yml：生产环境

敏感信息通过环境变量注入：

```text
DB_HOST
DB_PORT
DB_NAME
DB_USERNAME
DB_PASSWORD
DB_SSL_ENABLED
JWT_SECRET
AI_PROVIDER
AI_MODEL
AI_API_BASE_URL
AI_API_KEY
AI_ENABLED
```

配置原则：

- 不在代码里写死数据库连接地址
- 不在代码里写死 AI API Key
- 不把真实密钥提交到 Git 仓库
- 提供 .env.example 或 application-example.yml 作为配置模板
- 本地、服务器、云数据库只通过环境变量区分
- 云数据库如需 SSL 连接，通过 DB_SSL_ENABLED 控制
- 更换 AI API Key 时，只更新服务器环境变量或安全配置，不改代码

后端配置示例：

```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:dayliane}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

app:
  jwt:
    secret: ${JWT_SECRET}
  ai:
    enabled: ${AI_ENABLED:true}
    provider: ${AI_PROVIDER:openai}
    model: ${AI_MODEL}
    api-base-url: ${AI_API_BASE_URL}
    api-key: ${AI_API_KEY}
```

### 2.7 通知提醒

第一版建议支持：

- 网页站内消息通知
- 浏览器通知提醒
- 网页打开状态下的日程和任务提醒
- 后续可以扩展 PWA、浏览器推送、安卓本地提醒、厂商推送、极光推送或个推

## 3. 第一版核心功能

### 3.1 用户登录注册

功能说明：

- 用户可以注册账号
- 用户可以登录系统
- 用户可以退出登录
- 用户可以查看和修改个人资料

第一版建议登录方式：

- 手机号 + 密码

账号规则：

- 手机号作为用户唯一登录账号
- 第一版不支持用户自行换绑手机号
- 后续如需换绑手机号，需要增加身份验证流程

后续可扩展：

- 验证码登录
- 微信登录
- 邮箱登录

### 3.2 个人日程管理

功能说明：

- 新建个人日程
- 编辑个人日程
- 删除个人日程
- 设置日程时间
- 设置提醒时间
- 标记完成
- 查看今日日程
- 查看全部日程
- 在日历中查看指定日期的日程

删除规则：

- 第一版个人日程删除采用软删除
- 删除后默认不在普通列表中展示
- 后台管理员可按需要查看删除记录

日程字段建议：

- 标题
- 描述
- 开始时间
- 结束时间
- 提醒时间
- 完成状态
- 创建时间
- 更新时间

### 3.3 团队管理

功能说明：

- 创建团队
- 系统生成团队邀请码
- 用户通过邀请码加入团队
- 查看我加入的团队
- 查看团队成员
- 团队创建者可以管理团队成员
- 团队创建者可以设置或取消团队管理员
- 团队创建者和团队管理员可以移除普通成员

团队角色说明：

- owner：团队创建者，拥有团队最高权限
- admin：团队管理员，用于团队多级管理
- member：普通成员，可以接受、拒绝和完成分配给自己的任务

团队角色归属规则：

- 同一个用户可以在不同团队拥有不同角色
- 同一个用户在同一个团队内只能拥有一个角色
- admin 已包含 member 的基础能力
- owner 已包含 admin 和 member 的能力
- 角色权限关系为 owner > admin > member

团队成员管理规则：

- 团队创建者可以设置管理员、取消管理员、移除成员
- 团队管理员可以移除普通成员
- 团队管理员不能移除团队创建者
- 普通成员不能管理其他成员
- 团队创建者第一版不允许直接退出团队
- 团队转让功能后续版本再做
- 团队成员移除采用软删除，保留历史记录
- 被移除成员后续重新加入同一团队时，复用原 team_member 记录，将 status 从 removed 改回 active，并更新 joined_at

第一版团队加入方式：

- 邀请码

后续可扩展：

- 二维码邀请
- 链接邀请
- 手机号邀请

### 3.4 团队任务管理

功能说明：

- 创建团队任务
- 选择一个或多个执行人
- 设置任务截止时间
- 设置提醒时间
- 执行人可以接受或拒绝任务
- 执行人可以标记任务完成
- 创建者可以查看所有执行人的处理状态

任务状态说明：

- 执行人状态：待接受、已接受、已拒绝、已完成
- 团队任务业务状态：进行中、已完成、已取消
- 删除状态通过 deleted_at 判断，不和任务业务状态混用

重要规则：

- 一个团队任务可以同时分配给多个人
- 每个执行人都有独立的任务状态
- 某个成员拒绝任务，不影响其他成员接受或完成任务
- 创建者可以看到每个人的状态
- 被拒绝的任务可以重新分配给原执行人或其他团队成员
- 重新分配任务时，保留原执行人的拒绝记录，并新增新执行人的待接受记录
- 任务截止时间允许修改，但只有任务分配者或团队管理员可以修改
- 普通执行人不能修改任务截止时间
- 任务完成后第一版不允许普通成员自行撤回
- 如果任务完成状态需要修正，由任务分配者或团队管理员处理
- 团队任务删除采用软删除
- 只有任务创建者、团队管理员或团队创建者可以删除团队任务

团队任务整体状态规则：

- 只要存在未完成或待接受执行人，团队任务整体状态为进行中
- 所有执行人都完成后，团队任务整体状态可以标记为已完成
- 任务创建者、团队管理员或团队创建者可以取消团队任务
- 已取消任务不再允许执行人接受、拒绝或完成

### 3.5 首页总览

首页是用户自己的任务总界面。

首页混合显示：

- 我的个人日程
- 我被分配的团队任务

首页建议分区：

- 今日待办
- 即将到期
- 个人日程
- 团队任务

### 3.6 团队任务单独管理

除了首页总览，还需要单独的团队任务管理入口。

团队任务页面用于：

- 按团队查看任务
- 查看团队内所有任务
- 创建团队任务
- 查看成员接受、拒绝、完成情况

### 3.7 通知提醒

提醒对象：

- 被分配任务的执行人
- 任务创建者

通知场景：

- 有人给我分配任务
- 我负责的任务即将到期
- 成员接受任务
- 成员拒绝任务
- 成员完成任务
- 个人日程即将开始

### 3.8 AI 智能助手

AI 智能助手是第一版的创新功能，用于降低用户创建日程和管理任务的成本。

第一版建议支持：

- 自然语言创建日程
- AI 拆解团队任务
- AI 今日计划建议
- AI 任务描述优化

#### 3.8.1 自然语言创建日程

用户可以直接输入一句自然语言，例如：

```text
明天下午三点提醒我开会，提前半小时通知
```

AI 自动解析为结构化日程：

- 标题：开会
- 开始时间：明天下午 3 点
- 提醒时间：提前 30 分钟
- 类型：个人日程

用户确认后，系统再保存到日程表。

##### 时间类型判断

自然语言创建日程时，需要区分“持续型任务”和“瞬时型日程”。

###### 持续型任务

持续型任务指需要在一段时间内完成、以截止时间为核心的任务。

示例：

```text
8.31 前完成项目设计
下周五前写完调研报告
月底前整理完资料
三天内完成论文初稿
```

识别特点：

- 常见时间表达：8.31 前、下周五前、月底前、三天内、截止到某天
- 常见动作词：完成、写完、做完、整理完、提交前
- 重点字段是截止时间，不一定有明确开始时间
- 适合设置多次提醒

结构化结果示例：

```json
{
  "title": "完成项目设计",
  "timeType": "deadline_task",
  "startTime": null,
  "endTime": null,
  "deadlineTime": "2026-08-31 23:59:59",
  "remindTimes": [
    "2026-08-28 09:00:00",
    "2026-08-30 09:00:00",
    "2026-08-31 09:00:00"
  ],
  "needConfirm": true
}
```

提醒建议：

- 第一版默认截止前 1 天提醒
- 第一版默认截止当天 9:00 再次提醒
- 用户可以手动增加截止前 3 天或截止前 1 小时提醒

###### 瞬时型日程

瞬时型日程指在某一个明确时间点发生的事件。

示例：

```text
下午 6 点去提交材料
明天上午 9 点开会
周五晚上 7 点参加讨论
10 点上课
```

识别特点：

- 常见时间表达：下午 6 点、明天上午 9 点、周五晚上 7 点
- 常见动作词：去、开会、上课、面试、参加、提交、打电话
- 重点字段是开始时间
- 通常只需要提前一次或少量几次提醒

结构化结果示例：

```json
{
  "title": "提交材料",
  "timeType": "point_event",
  "startTime": "2026-07-20 18:00:00",
  "endTime": null,
  "deadlineTime": null,
  "remindTimes": [
    "2026-07-20 17:30:00"
  ],
  "needConfirm": true
}
```

提醒建议：

- 默认提前 30 分钟提醒
- 用户也可以选择提前 10 分钟、1 小时或自定义时间提醒

判断规则说明：

- 出现“前、之前、截止、截至、内”等表达时，优先判断为持续型任务
- 出现明确时间点，如“下午 6 点、明天 9 点、周五上午”时，优先判断为瞬时型日程
- “提交”这类词需要结合时间表达判断，例如“周五前提交报告”属于持续型任务，“下午 6 点提交材料”属于瞬时型日程
- AI 解析结果必须由用户确认后再保存，避免时间识别错误直接写入数据库
- 如果 AI 解析失败、超时或返回格式错误，前端提示用户手动填写，不影响用户继续创建日程

#### 3.8.2 AI 拆解团队任务

用户创建团队任务时，可以让 AI 根据任务目标自动生成子任务建议。

示例：

```text
下周五前完成小组调研报告
```

AI 可以建议拆解为：

- 确定调研主题
- 分配资料收集人员
- 整理调研数据
- 编写报告初稿
- 检查并提交最终版本

第一版可以先作为辅助建议展示，由创建者手动确认是否采用。

AI 拆解失败处理：

- AI 拆解失败时，前端提示用户手动填写任务描述和执行人
- AI 功能失败不影响普通团队任务创建流程

#### 3.8.3 AI 今日计划建议

首页增加 AI 今日建议区域，根据用户当天日程、即将截止任务、未完成团队任务生成优先级建议。

示例：

```text
今天建议优先完成 3 件事：
1. 上午确认团队任务反馈
2. 下午完成课程作业初稿
3. 晚上检查明天会议资料
```

AI 今日计划失败处理：

- AI 生成失败时，首页隐藏 AI 建议或显示“今日建议暂不可用”
- 首页基础待办列表仍然正常展示

#### 3.8.4 AI 任务描述优化

用户输入简单任务描述后，AI 可以帮助补全更清晰的任务说明。

示例：

```text
做一下报告
```

优化为：

```text
完成项目调研报告，包括背景介绍、数据分析、结论建议三部分，并在截止时间前提交。
```

AI 优化失败处理：

- AI 优化失败时，保留用户原始输入内容
- 用户仍然可以手动保存任务

## 4. 网页端页面规划

### 4.1 登录注册模块

页面：

- 登录页
- 注册页
- 忘记密码页

### 4.2 首页模块

页面：

- 首页 / 我的任务总览

首页内容：

- 今日个人日程
- 今日团队任务
- 即将到期任务
- 快速新建入口
- 未读消息提示
- AI 今日计划建议

### 4.3 日历模块

页面：

- 日历页
- 某日任务列表页

功能：

- 按日期查看任务
- 同时显示个人日程和团队任务
- 点击某一天查看当天详细安排

### 4.4 个人日程模块

页面：

- 个人日程列表页
- 新建日程页
- 编辑日程页
- 日程详情页

### 4.5 团队模块

页面：

- 团队列表页
- 创建团队页
- 加入团队页
- 团队详情页
- 团队成员页

### 4.6 团队任务模块

页面：

- 团队任务列表页
- 创建团队任务页
- 团队任务详情页
- 执行人状态页

### 4.7 消息通知模块

页面：

- 消息通知列表页
- 消息详情页

### 4.8 AI 助手模块

页面 / 入口：

- AI 快速创建日程入口
- 创建团队任务页中的 AI 拆解任务按钮
- 任务描述输入框中的 AI 优化按钮
- 首页 AI 今日计划建议区域

功能：

- 用户输入自然语言后，AI 解析为日程草稿
- 用户输入任务目标后，AI 生成子任务建议
- 用户输入简单描述后，AI 优化为更完整的任务说明
- 用户进入首页后，AI 根据当天任务生成优先级建议

### 4.9 我的模块

页面：

- 我的页面
- 个人资料页
- 修改密码页
- 通知设置页
- 退出登录

## 5. 用户流程

### 5.1 个人日程流程

```text
用户注册/登录
进入首页
点击新建
选择个人日程
填写标题、时间、提醒时间和备注
保存日程
首页和日历中显示该日程
到提醒时间后收到通知
完成后标记为已完成
```

### 5.2 创建团队流程

```text
用户登录
进入团队页面
点击创建团队
填写团队名称
创建成功
系统生成邀请码
用户把邀请码发给其他成员
```

### 5.3 加入团队流程

```text
用户登录
进入团队页面
点击加入团队
输入邀请码
系统校验邀请码
加入成功
团队出现在团队列表中
```

### 5.4 团队任务分配流程

```text
创建者进入团队详情页
点击创建团队任务
填写任务标题、描述、截止时间、提醒时间
选择一个或多个执行人
提交任务
系统给执行人发送任务通知
执行人在消息或首页中看到任务
执行人选择接受或拒绝
创建者收到成员接受或拒绝的通知
执行人完成任务后标记完成
创建者收到完成通知
```

## 6. 数据库设计初稿

### 6.1 user 用户表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 用户 ID |
| phone | varchar | 手机号，唯一 |
| password_hash | varchar | 密码加密值 |
| nickname | varchar | 昵称 |
| avatar_url | varchar | 头像 |
| status | int | 用户状态 |
| deleted_at | datetime | 删除时间，软删除使用 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 6.2 team 团队表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 团队 ID |
| name | varchar | 团队名称 |
| invite_code | varchar | 邀请码 |
| owner_id | bigint | 创建者用户 ID |
| status | varchar | 团队状态：active/deleted |
| deleted_at | datetime | 删除时间，软删除使用 |
| deleted_by | bigint | 删除操作者用户 ID |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 6.3 team_member 团队成员表

约束说明：

- team_id + user_id 唯一，避免同一个用户在同一个团队中出现多条成员记录
- 用户在不同团队可以拥有不同角色
- 用户在同一个团队内只能拥有一个角色

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 记录 ID |
| team_id | bigint | 团队 ID |
| user_id | bigint | 用户 ID |
| role | varchar | 角色：owner/admin/member |
| status | varchar | 成员状态：active/removed |
| joined_at | datetime | 加入时间 |
| removed_at | datetime | 移除时间 |
| removed_by | bigint | 移除操作者用户 ID |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 6.4 schedule 个人日程表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 日程 ID |
| user_id | bigint | 用户 ID |
| title | varchar | 标题 |
| description | text | 描述 |
| time_type | varchar | 时间类型：point_event/deadline_task |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| deadline_time | datetime | 截止时间，持续型任务使用 |
| remind_time | datetime | 提醒时间 |
| remind_times | json | 多次提醒时间，持续型任务可使用 |
| status | varchar | 状态：pending/completed/cancelled |
| deleted_at | datetime | 删除时间，软删除使用 |
| deleted_by | bigint | 删除操作者用户 ID |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

提醒字段说明：

- remind_time 表示主要提醒时间，用于列表排序和快速查询
- remind_times 表示多次提醒时间，持续型任务可存多个提醒时间
- 只有一次提醒时，remind_time 和 remind_times 中的唯一时间保持一致

### 6.5 team_task 团队任务表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 任务 ID |
| team_id | bigint | 团队 ID |
| creator_id | bigint | 创建者用户 ID |
| title | varchar | 任务标题 |
| description | text | 任务描述 |
| deadline_time | datetime | 截止时间 |
| remind_time | datetime | 提醒时间 |
| status | varchar | 任务业务状态：active/completed/cancelled |
| updated_by | bigint | 最近修改任务的用户 ID |
| deadline_updated_at | datetime | 截止时间最近修改时间 |
| deleted_at | datetime | 删除时间，软删除使用 |
| deleted_by | bigint | 删除操作者用户 ID |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

### 6.6 team_task_assignee 团队任务执行人表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 记录 ID |
| task_id | bigint | 任务 ID |
| user_id | bigint | 执行人用户 ID |
| status | varchar | 状态：pending/accepted/rejected/completed |
| accepted_at | datetime | 接受时间 |
| rejected_at | datetime | 拒绝时间 |
| completed_at | datetime | 完成时间 |
| reassigned_from_user_id | bigint | 重新分配来源用户 ID，可为空 |
| assigned_by | bigint | 分配或重新分配操作者用户 ID |
| status_updated_by | bigint | 最近修改状态的用户 ID |

### 6.7 notification 通知表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 通知 ID |
| user_id | bigint | 接收通知的用户 ID |
| type | varchar | 通知类型 |
| title | varchar | 通知标题 |
| content | text | 通知内容 |
| related_type | varchar | 关联对象类型 |
| related_id | bigint | 关联对象 ID |
| is_read | boolean | 是否已读 |
| deleted_at | datetime | 删除时间，软删除使用 |
| created_at | datetime | 创建时间 |

### 6.8 admin_user 后台管理员表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 管理员 ID |
| username | varchar | 用户名 |
| password_hash | varchar | 密码加密值 |
| role | varchar | 管理员角色 |
| created_at | datetime | 创建时间 |

### 6.9 ai_usage_log AI 调用记录表

用于记录 AI 功能调用情况，方便后续统计使用量、排查问题和控制成本。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 记录 ID |
| user_id | bigint | 调用用户 ID |
| feature_type | varchar | AI 功能类型：schedule_parse/task_breakdown/daily_plan/description_optimize |
| input_text | text | 用户输入内容 |
| output_text | text | AI 返回内容 |
| status | varchar | 调用状态：success/failed |
| error_message | text | 失败原因 |
| created_at | datetime | 创建时间 |

### 6.10 ai_config AI 配置表

用于管理 AI 服务基础配置。第一版也可以只使用环境变量，配置表用于后续管理后台扩展。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 配置 ID |
| provider | varchar | AI 服务商，例如 openai/qwen/zhipu/deepseek |
| model_name | varchar | 模型名称 |
| api_base_url | varchar | AI 接口基础地址 |
| api_key_masked | varchar | 脱敏后的密钥展示，例如 sk-****abcd |
| enabled | boolean | 是否启用 |
| remark | varchar | 备注 |
| created_at | datetime | 创建时间 |
| updated_at | datetime | 更新时间 |

安全说明：

- 不建议在数据库中明文保存完整 API Key
- 第一版建议 API Key 使用服务器环境变量管理
- 如果后续必须保存到数据库，需要加密存储，并且后台只展示脱敏结果

### 6.11 admin_operation_log 管理员操作日志表

用于记录后台管理员的重要操作，方便审计和问题追踪。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 日志 ID |
| admin_id | bigint | 操作管理员 ID |
| action | varchar | 操作类型，例如 update_ai_config/update_user_status/update_task_status |
| target_type | varchar | 操作对象类型 |
| target_id | bigint | 操作对象 ID |
| before_data | json | 修改前数据 |
| after_data | json | 修改后数据 |
| ip_address | varchar | 操作 IP |
| created_at | datetime | 创建时间 |

### 6.12 reminder_record 提醒记录表

用于记录已经生成或发送过的提醒，避免重复提醒。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | bigint | 记录 ID |
| user_id | bigint | 接收提醒用户 ID |
| related_type | varchar | 关联对象类型：schedule/team_task |
| related_id | bigint | 关联对象 ID |
| remind_time | datetime | 计划提醒时间 |
| status | varchar | 提醒状态：pending/sent/failed |
| sent_at | datetime | 实际发送时间 |
| error_message | text | 失败原因 |
| created_at | datetime | 创建时间 |

## 7. 后端接口初稿

### 7.1 用户接口

- POST /api/v1/auth/register 注册
- POST /api/v1/auth/login 登录
- POST /api/v1/auth/logout 退出登录
- GET /api/v1/user/profile 获取个人资料
- PUT /api/v1/user/profile 修改个人资料

### 7.2 个人日程接口

- POST /api/v1/schedules 创建日程
- GET /api/v1/schedules 查询日程列表
- GET /api/v1/schedules/{id} 查询日程详情
- PUT /api/v1/schedules/{id} 修改日程
- DELETE /api/v1/schedules/{id} 删除日程
- PUT /api/v1/schedules/{id}/complete 标记完成

### 7.3 团队接口

- POST /api/v1/teams 创建团队
- GET /api/v1/teams 查询我的团队
- GET /api/v1/teams/{id} 查询团队详情
- POST /api/v1/teams/join 通过邀请码加入团队
- GET /api/v1/teams/{id}/members 查询团队成员
- PUT /api/v1/teams/{teamId}/members/{userId}/role 修改团队成员角色
- DELETE /api/v1/teams/{teamId}/members/{userId} 移除团队成员

权限规则：

- 只有团队创建者可以设置或取消管理员
- 团队创建者和团队管理员可以移除普通成员
- 团队管理员不能移除团队创建者
- 普通成员不能修改团队成员角色，也不能移除成员

### 7.4 团队任务接口

- POST /api/v1/team-tasks 创建团队任务
- GET /api/v1/team-tasks/my 查询我负责的团队任务
- GET /api/v1/teams/{teamId}/tasks 查询某团队任务
- GET /api/v1/team-tasks/{id} 查询任务详情
- PUT /api/v1/team-tasks/{id} 修改任务
- PUT /api/v1/team-tasks/{id}/deadline 修改任务截止时间
- DELETE /api/v1/team-tasks/{id} 删除任务
- POST /api/v1/team-tasks/{id}/accept 接受任务
- POST /api/v1/team-tasks/{id}/reject 拒绝任务
- POST /api/v1/team-tasks/{id}/complete 完成任务
- POST /api/v1/team-tasks/{id}/cancel 取消任务
- POST /api/v1/team-tasks/{id}/reassign 重新分配任务
- PUT /api/v1/team-tasks/{id}/assignees/{userId}/status 管理员修正执行人任务状态

权限规则：

- 任务分配者和团队管理员可以修改任务截止时间
- 普通执行人不能修改任务截止时间
- 被拒绝的任务可以由任务分配者或团队管理员重新分配
- 普通成员完成任务后，第一版不支持自行撤回
- 如需修正已完成状态，由任务分配者或团队管理员通过状态修正接口处理
- 任务创建者、团队管理员或团队创建者可以取消任务
- 已取消任务不允许执行人继续接受、拒绝或完成

### 7.5 首页接口

- GET /api/v1/home/today 查询今日总览
- GET /api/v1/home/upcoming 查询即将到期任务

### 7.6 通知接口

- GET /api/v1/notifications 查询通知列表
- PUT /api/v1/notifications/{id}/read 标记已读
- PUT /api/v1/notifications/read-all 全部标记已读

### 7.7 提醒调度接口

提醒调度主要由后端定时任务自动执行，普通用户端不直接调用。

- POST /api/v1/admin/reminders/scan 手动触发提醒扫描
- GET /api/v1/admin/reminders/records 查询提醒记录

后端提醒模块规则：

- 后端增加 reminder 模块，定时扫描即将到期的个人日程和团队任务
- 扫描到需要提醒的记录后，生成 notification 和 reminder_record
- reminder_record 用于避免同一提醒时间重复发送
- 第一版先生成网页站内通知，浏览器通知由前端在用户在线时展示

### 7.8 AI 智能助手接口

- POST /api/v1/ai/schedules/parse 自然语言解析日程
- POST /api/v1/ai/team-tasks/breakdown 拆解团队任务
- POST /api/v1/ai/home/daily-plan 生成今日计划建议
- POST /api/v1/ai/text/optimize-task-description 优化任务描述

接口设计建议：

- AI 接口只返回草稿或建议，不直接修改数据库中的日程和任务
- 用户确认后，再调用普通日程或任务接口保存
- 后端需要记录 AI 调用日志，便于后续限制调用次数和排查错误

### 7.9 AI 管理接口

- GET /api/v1/admin/ai/config 查询 AI 配置状态
- PUT /api/v1/admin/ai/config 修改 AI 配置
- PUT /api/v1/admin/ai/enabled 启用或关闭 AI 功能
- GET /api/v1/admin/ai/usage-logs 查询 AI 调用记录
- GET /api/v1/admin/ai/usage-stats 查询 AI 调用统计
- POST /api/v1/admin/ai/test 测试 AI 服务是否可用

权限规则：

- AI 管理接口只允许后台管理员调用
- 后台不返回完整 API Key，只返回脱敏后的密钥信息
- 修改 AI 配置需要记录管理员操作日志
- 用户端 AI 接口需要登录后才能调用

### 7.10 管理员操作日志接口

- GET /api/v1/admin/operation-logs 查询管理员操作日志

权限规则：

- 仅后台管理员可查看操作日志
- 修改 AI 配置、用户状态、团队任务状态、执行人状态时需要记录操作日志

## 8. Vue 管理后台规划

后台第一版主要用于管理员查看和处理数据。

### 8.1 后台页面

- 管理员登录页
- 首页数据概览
- 用户管理
- 团队管理
- 团队成员管理
- 个人日程管理
- 团队任务管理
- 通知记录管理
- 提醒记录管理
- AI 调用记录管理
- AI 配置管理
- 管理员操作日志
- 管理员账号管理

### 8.2 后台功能

- 查看用户列表
- 禁用或启用用户
- 查看团队列表
- 查看团队成员
- 查看个人日程
- 查看团队任务
- 查看通知记录
- 查看提醒记录
- 查看 AI 调用记录
- 查看 AI 服务启用状态
- 启用或关闭 AI 功能
- 测试 AI 服务是否可用
- 修改 AI 服务商和模型配置
- 查看管理员操作日志
- 管理后台管理员账号

## 9. 开发阶段规划

### 第一阶段：基础架构

目标：

- 搭建 Spring Boot 后端项目
- 搭建 MySQL 数据库
- 搭建 Vue 用户网页端项目
- 搭建 Vue 后台项目
- 完成登录注册基础能力
- 完成后端多环境配置
- 完成数据库和 AI Key 环境变量配置模板

### 第二阶段：个人日程

目标：

- 完成个人日程增删改查
- 完成首页今日待办
- 完成日历查看
- 完成网页站内提醒
- 完成浏览器通知提醒

### 第三阶段：团队功能

目标：

- 完成创建团队
- 完成邀请码加入团队
- 完成团队成员列表
- 完成团队管理员角色管理
- 完成团队成员移除

### 第四阶段：团队任务

目标：

- 完成团队任务创建
- 支持选择多个执行人
- 支持执行人接受、拒绝、完成
- 支持创建者查看所有执行人的状态
- 支持被拒绝任务重新分配
- 支持分配者或管理员修改任务截止时间

### 第五阶段：通知提醒

目标：

- 完成网页站内通知
- 完成任务分配通知
- 完成状态变化通知
- 完成任务到期提醒
- 完成 reminder 后端定时提醒扫描
- 完成提醒记录保存，避免重复提醒

### 第六阶段：AI 智能助手

目标：

- 完成自然语言创建日程
- 完成 AI 拆解团队任务
- 完成 AI 今日计划建议
- 完成 AI 任务描述优化
- 完成 AI 调用记录保存
- 完成 AI 配置读取和功能开关
- 完成 AI 服务可用性测试接口

### 第七阶段：后台管理

目标：

- 完成 Vue 后台基础页面
- 完成用户、团队、任务、通知、提醒记录、AI 调用记录、AI 配置、管理员操作日志管理

### 第八阶段：测试和优化

目标：

- 修复功能问题
- 优化页面体验
- 测试登录、日程、团队、任务、提醒完整流程
- 测试 AI 日程解析、任务拆解和今日建议流程
- 准备网页端上线部署

## 10. 第一版暂不开发的功能

为了控制开发难度，以下功能建议后续版本再做：

- iOS 版本
- 微信登录
- 短信验证码登录
- 支付功能
- 聊天功能
- 文件附件
- 复杂权限系统
- 项目甘特图
- 复杂 AI 自动排程
- 安卓原生 App
- PWA 离线能力和关闭网页后的推送提醒
- 企业级审批流程
- 多语言

## 11. 已确认规则与待确认问题

### 11.1 已确认规则

- 团队成员需要管理员角色，用于团队多级管理
- 同一用户可以在不同团队拥有不同角色，但在同一个团队内只能拥有一个角色
- 团队创建者可以移除团队成员
- 被拒绝的任务可以重新分配
- 任务截止时间允许修改，但只有任务分配者或团队管理员可以修改
- 任务完成后如果误操作，只能由任务分配者或团队管理员把状态改回已接受
- 后台管理系统只给管理员使用
- AI 功能暂时不限制每日调用次数
- AI 生成内容需要用户确认后再保存
- AI 调用记录需要在后台展示给管理员
- AI API Key 不在前端保存或展示
- AI API Key 第一版使用服务器环境变量管理
- 后台只展示 AI 服务状态、功能开关、调用记录和脱敏后的配置信息
- 数据库连接信息通过环境变量和多环境配置管理
- 后续从本地数据库切换到云数据库时，不修改业务代码
- 第一版不使用短信验证码登录，采用手机号 + 密码登录
- 手机号作为用户唯一登录账号
- 第一版不支持用户自行换绑手机号
- 团队管理员和后台管理员是两套不同角色
- 团队管理员不能登录系统后台
- 团队创建者第一版不允许直接退出团队
- 团队转让功能后续版本再做
- 重要业务数据采用软删除，包括个人日程、团队、团队任务、团队成员关系和通知
- 被移除成员重新加入同一团队时，复用原 team_member 记录并恢复为 active
- 任务重新分配时，保留原执行人的拒绝记录，并新增新执行人的待接受记录
- 团队任务只有任务创建者、团队管理员或团队创建者可以删除
- 团队任务状态和软删除状态分开处理，deleted_at 只表示是否删除
- 瞬时型日程默认提前 30 分钟提醒
- 持续型任务默认截止前 1 天和截止当天 9:00 提醒
- remind_time 表示主要提醒时间，remind_times 表示多次提醒时间
- 后端增加 reminder 模块，负责定时扫描提醒并生成通知
- 接口统一使用 /api/v1 前缀
- 后台重要操作需要记录 admin_operation_log
- AI 调用失败时，不影响用户手动创建日程或任务
- AI 调用日志第一版保存用户输入和 AI 输出全文，仅后台管理员可查看
- 云数据库连接预留 SSL 配置项

### 11.2 手机号登录与短信验证码

手机号登录可以分为两种方式：

- 手机号 + 密码：实现简单，不需要短信服务，适合第一版快速开发
- 手机号 + 短信验证码：体验更方便，但需要接入第三方短信服务

如果需要短信验证码，需要准备：

- 短信服务商账号，例如阿里云短信、腾讯云短信、华为云短信等
- 已备案或可用于短信签名审核的主体信息
- 短信签名，例如“日程提醒”
- 短信模板，例如“您的验证码是 ${code}，5 分钟内有效”
- 短信接口密钥，用于后端调用短信服务
- 验证码存储方案，可以使用 Redis 或数据库临时表
- 发送频率限制，例如同一手机号 60 秒内只能发送一次

第一版规则：

- 使用手机号 + 密码登录
- 暂时不接入短信验证码
- 短信验证码登录作为后续增强功能

### 11.3 任务完成后是否允许撤回

“任务完成后撤回”指执行人已经把任务标记为已完成，但后来发现误操作或任务还没真正完成，需要从“已完成”改回“进行中 / 已接受”。

示例：

```text
成员误点了“完成任务”
后来发现材料还没提交
此时是否允许把任务状态从已完成撤回为已接受
```

可选规则：

- 不允许撤回：流程简单，状态更稳定
- 允许执行人在短时间内撤回：例如完成后 10 分钟内可以撤回
- 允许管理员或分配者撤回：适合团队管理，但规则更复杂

第一版规则：

- 暂时不允许普通成员自行撤回
- 如需修正，由任务分配者或团队管理员将任务状态改回已接受

### 11.4 网页站内提醒、浏览器通知与 PWA 推送

网页提醒可以分为三层：

#### 网页站内提醒

用户打开系统网页时，在页面内显示消息、红点、弹窗或通知列表。

特点：

- 实现最简单
- 不需要浏览器授权
- 用户关闭网页后无法提醒

#### 浏览器通知

用户打开网页并授权通知权限后，系统可以通过浏览器弹出系统级通知。

特点：

- 用户在当前浏览器中可以收到更明显的提醒
- 需要用户授权通知权限
- 通常要求网站使用 HTTPS
- 如果网页完全关闭，提醒能力会受限制

#### PWA 推送

将网页做成接近 App 的 PWA，配合 Service Worker 和 Web Push，在网页关闭后也尽可能推送通知。

特点：

- 体验更接近 App
- 技术复杂度更高
- 需要 HTTPS、Service Worker、Push 订阅和后端推送支持
- 不同浏览器和设备支持程度不同

第一版建议：

- 先做网页站内提醒
- 再做浏览器通知
- PWA 推送放到后续版本

## 12. 推荐第一版名称

日程提醒 Web V1：个人日程 + 团队任务协作 + AI 智能助手版

