# 测试与联调验收 - 未完成项

## 1. 后端业务测试

当前缺少业务测试，需要补充以下测试。

### 1.1 登录/注册测试

- 正常注册
- 重复手机号注册失败
- 正确密码登录成功
- 错误密码登录失败
- refresh token 轮换
- logout 后 token 失效
- 登录限流触发

### 1.2 日程状态测试

- 创建日程
- 完成日程
- 取消日程
- 恢复已取消日程
- 已完成日程不可重复完成
- 已取消日程不可直接完成
- 日程完成/取消后 pending reminder 自动取消

### 1.3 团队角色权限测试

- 创建团队后创建者成为 owner
- 成员通过邀请码加入团队
- owner 设置 admin
- owner/admin 移除成员
- 普通 member 不能移除成员
- 被移除成员不能再访问团队
- 成员重新加入团队后不自动恢复旧任务责任

### 1.4 团队任务状态流转测试

- 创建任务后执行人为 pending
- 执行人接受任务
- 执行人拒绝任务
- 执行人完成任务
- 全部 completed 后整体为 completed
- 全部 rejected 后整体为 all_rejected
- completed + rejected 且无 pending/accepted 后整体为 completed
- all_rejected 后重新分配变回 active
- 取消任务
- 恢复任务后重新计算整体状态
- rejected 执行人重新分配后旧记录 `is_active=false`

### 1.5 提醒与通知测试

- 日程提醒触发生成通知
- 团队任务提醒为每个执行人生成通知
- 执行人拒绝后只取消该执行人的提醒
- 团队任务完成/取消后取消 pending reminder
- 扫描任务不重复发送同一提醒

## 2. 联调验收

按 A/B/C 用户流程完整验收。

### A 用户流程

- 注册/登录 A
- 创建团队并获取邀请码
- 创建个人日程
- 创建团队任务并分配给 B、C
- 查看提醒和通知

### B 用户流程

- 注册/登录 B
- 通过邀请码加入团队
- 查看团队任务
- 接受任务
- 完成任务

### C 用户流程

- 注册/登录 C
- 通过邀请码加入团队
- 查看团队任务
- 拒绝任务
- 验证团队任务整体状态

### 管理员流程

- 使用管理员账号登录后台
- 查看用户/团队/日程/任务/通知/提醒详情
- 使用搜索、筛选、分页
- 禁用/启用普通用户
- 创建/禁用管理员
- 查看操作日志

## 3. MVP 完成标准验证

- 个人日程：创建、编辑、完成、取消、恢复、删除、日历展示、详情查看
- 团队：创建、加入、成员管理、权限按钮正确
- 团队任务：创建、分配、接受、拒绝、完成、重新分配、取消、恢复、删除、状态自动计算
- 提醒：自动扫描并生成通知
- 通知：列表、详情、标记已读
- 管理后台：详情页、搜索、筛选、分页、管理员账号、操作日志

## 4. 2026-07-26 自动化验收记录

### 已补充并通过

- 后端业务验收测试：新增 `BusinessAcceptanceTests`，覆盖注册/重复注册、正确/错误登录、refresh token 轮换、logout 后 access token 失效、登录限流。
- 日程状态：覆盖创建、完成、取消、恢复、重复完成拦截、取消后直接完成拦截、完成/取消后 pending reminder 自动取消。
- 团队权限：覆盖创建团队 owner、邀请码加入、owner 设置 admin、owner/admin 移除成员、member 无权移除、被移除成员不可访问、重新加入不恢复旧任务责任。
- 团队任务状态：覆盖 pending、accept、reject、complete、全部完成、全部拒绝、完成+拒绝、all_rejected 后重分配为 active、取消、恢复、rejected 重分配后旧记录 `is_active=false`。
- 提醒与通知：覆盖日程提醒扫描生成通知、团队任务为每个执行人生成提醒、拒绝后只取消该执行人的 pending reminder、任务完成后取消 pending reminder、重复扫描不重复发送。

### 执行结果

- `cd src/backend && .\mvnw.cmd clean test`：通过，5 tests / 0 failures / 0 errors。
- `cd src/web-user && npm run build`：通过。
- `cd src/admin-web && npm run build`：通过。

### 仍需手工联调

- A/B/C 用户完整浏览器流程：注册登录、团队创建/加入、任务接受/拒绝/完成、通知查看。
- 管理后台浏览器流程：详情页、搜索、筛选、分页、禁用/启用用户、管理员账号管理、操作日志查看。