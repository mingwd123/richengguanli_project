# 管理员体系 - 已完成

## 1. 独立管理员登录

- 新增 `POST /api/v1/admin/auth/login`
- 使用 `admin_user` 表校验管理员账号
- 使用 BCrypt 校验 `admin_user.password_hash`
- 签发管理员专用 token，token 中需要能区分管理员身份
- admin-web 登录页改为调用管理员登录接口

## 2. 管理员角色校验

- 增加管理员身份校验方法，例如 `requireAdmin(...)`
- `AdminController` 所有接口必须校验管理员身份
- 普通用户 token 访问 `/api/v1/admin/**` 应返回 403

## 3. 管理员操作日志写入

- 新增 `AdminOperationLog` 数据访问逻辑
- 管理员写操作需要写入 `admin_operation_log`
- 至少覆盖：启用/禁用用户、创建管理员、禁用管理员
- 记录：admin_id、action、target_type、target_id、before_data、after_data、ip_address、user_agent

## 4. 管理员账号管理接口

- 新增管理员列表接口
- 新增管理员创建接口
- 新增管理员启用/禁用接口
- 后续可补充管理员修改密码接口

已完成
