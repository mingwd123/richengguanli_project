# V1.1 开发计划：AI 与体验增强版

基于 MVP 已全部完成，V1.1 在现有基础上新增 AI 功能和 DeskHive 体验增强。

---

## 一、数据库现状

分组与 AI 所需表/字段已在 MVP 阶段预先创建；通知偏好使用独立的 V8 迁移补充：

| 表/字段 | 状态 |
|---------|------|
| `task_group` 表 | 已存在 |
| `schedule.group_id` / `schedule.sort_order` | 已存在 |
| `team_task.group_id` / `team_task.sort_order` | 已存在 |
| `ai_usage_log` 表 | 已存在 |
| `ai_config` 表 | 已存在 |
| `notification_preference` 表 | V8 已新增 |

### AI 默认配置

| 配置项 | 值 |
|--------|-----|
| 服务商 | `deepseek` |
| 模型 | `deepseek-v4-flash` |
| API Base URL | `https://api.deepseek.com/v1` |
| API Key | `<通过 AI_API_KEY 环境变量注入>` |

> API Key 仅通过环境变量 `AI_API_KEY` 注入，不在文档或代码中保存真实值。历史泄露的密钥必须在服务商控制台轮换。

需新增的环境变量（由后端读取）：
- `AI_API_KEY`：无默认值，由部署环境提供
- `AI_API_BASE_URL`：默认值 `https://api.deepseek.com/v1`
- `AI_MODEL`：默认值 `deepseek-v4-flash`
- `REMINDER_SCAN_TOKEN`：内部提醒扫描接口密钥，生产环境必须显式配置

---

## 二、开发阶段

### 阶段 1：分组与排序增强（用户体验基础）— 已完成

> 已完成个人/团队分组管理、分组排序、任务移组和组内上移/下移排序；拖拽排序与右键菜单留待后续体验增强阶段。

#### 后端 — 已完成
- [x] `TaskGroupController` 个人分组接口：创建、查询、重命名、删除、排序。
- [x] 团队任务分组接口：查询、创建、重命名、删除、排序。
- [x] 个人日程、团队任务的移组和组内排序接口。
- [x] 分组重命名同步事项分组名称；删除分组自动迁移其中事项。
- [x] 团队分组管理与团队任务排序均已完成对应权限校验。

#### 用户端 — 已完成
- [x] 个人日程和团队任务按持久化分组顺序展示。
- [x] 分组支持上移、下移、重命名、删除；删除前确认。
- [x] 个人日程和有管理权限的团队任务支持“移动至”其他分组。
- [x] 个人日程和有管理权限的团队任务支持组内上移/下移排序。
- [x] `/分组名` 快捷输入创建分组。
- [x] 分组及任务卡片右键菜单。
- [x] 分组内拖拽排序、跨分组拖拽和已完成分组排序。

---

### 阶段 2：AI 后端服务 — 已完成

#### AI 模块 — 已完成
- [x] 新增 `AiService`，封装 DeepSeek API 调用（兼容 OpenAI 协议，后续可切换服务商）：
  - 默认使用 DeepSeek（`deepseek-v4-flash` 模型，`https://api.deepseek.com/v1`）
  - 构造 system prompt + user prompt
  - 调用 `/v1/chat/completions`
  - 解析返回 JSON，容错处理
- [x] 新增 `AiController`：
  - `POST /api/v1/ai/schedules/parse` — 自然语言解析日程（返回结构化草稿）
  - `POST /api/v1/ai/team-tasks/breakdown` — 拆解团队任务（返回子任务列表）
  - `POST /api/v1/ai/home/daily-plan` — 今日计划建议（基于现有日程+任务）
  - `POST /api/v1/ai/text/optimize-task-description` — 优化任务描述
- [x] AI 调用记录写入 `ai_usage_log`，遵守隐私规则（用户可关闭记录）
- [x] AI 接口只返回建议/草稿，**不直接写库**，用户确认后再调原有接口保存

#### AI 管理接口（后台） — 已完成
- [x] 新增 `AdminAiController`：
  - `GET /api/v1/admin/ai/config` 查询 AI 配置
  - `PUT /api/v1/admin/ai/config` 修改 AI 配置（记录操作日志）
  - `PUT /api/v1/admin/ai/enabled` 启用/关闭 AI
  - `GET /api/v1/admin/ai/usage-logs` 查询调用记录（分页+筛选）
  - `GET /api/v1/admin/ai/usage-stats` 调用统计
  - `POST /api/v1/admin/ai/test` 测试 AI 可用性

---

### 阶段 3：AI 前端集成 — 已完成

#### 用户端 — 已完成
- [x] 创建/编辑日程页：增加「AI 解析」按钮，输入自然语言后自动填充表单
- [x] 创建团队任务页：增加「AI 拆解」按钮，输入目标后生成子任务列表
- [x] 首页：新增「AI 今日计划」建议区域，基于当前日程状态生成建议
- [x] 任务描述输入框：增加「AI 优化」按钮，润色描述文案
- [x] 个人设置页：增加「AI 数据记录」开关（localStorage 持久化）

#### 管理后台 — 已完成
- [x] 新增「AI 配置」页面：服务商选择、模型名称、API Base URL、启用开关、测试按钮
- [x] 新增「AI 调用记录」页面：列表、搜索、筛选、详情查看

---

### 阶段 4：主题与动效增强 — 已完成

#### 用户端 — 已完成
- [x] 日间/夜间主题切换（CSS 变量方案，light/dark）
- [x] 主题偏好持久化到 localStorage
- [x] 任务完成划线动画、卡片淡入淡出、分组折叠展开过渡
- [x] 拖拽过程视觉反馈相关基础动效与低干扰 hover 反馈

#### 管理后台 — 已完成
- [x] 同步支持日间/夜间主题切换

---

### 阶段 5：浏览器通知 — 已完成

- [x] 前端请求浏览器通知权限
- [x] 收到站内通知时同步弹出浏览器通知（生产环境需 HTTPS）
- [x] 点击浏览器通知跳转到对应页面
- [x] 浏览器通知轮询在页面隐藏时继续工作
- [x] 新增通知设置页，分别管理浏览器、任务分配、任务状态和到期提醒
- [x] 通知偏好持久化到后端，并在通知创建与提醒扫描时生效

---

## 三、新增页面清单

### 用户端新增

| 页面/组件 | 说明 | 状态 |
|-----------|------|------|
| AI 日程解析入口 | 嵌入 `SchedulesPage.vue` 模态框，新增 AI 按钮 + 自动填充表单 | 已完成 |
| AI 任务拆解入口 | 嵌入 `TasksPage.vue` 创建表单 | 已完成 |
| 首页 AI 计划建议区 | `HomePage.vue` 新增 AI 计划模块 | 已完成 |
| AI 描述优化按钮 | 嵌入 `TeamTaskDetailPage.vue` 描述区域 | 已完成 |
| AI 数据记录开关 | 嵌入 `ProfileSettingsPage.vue` | 已完成 |
| 通知偏好设置 | `/profile/notifications` | 已完成 |
| 右键菜单组件 | 新增 `ContextMenu.vue`（任务卡片 + 分组标题） | 已完成 |
| 拖拽排序交互 | 分组列表 + 任务卡片排序 | 已完成 |
| 主题切换按钮 | 顶部导航或侧边栏新增入口 | 已完成 |

### 管理后台新增

| 页面 | 路由 | 状态 |
|------|------|------|
| AI 配置管理 | `/admin/ai-config` | 已完成 |
| AI 调用记录 | `/admin/ai-logs` | 已完成 |

---

## 四、后端新增文件

```
src/backend/src/main/java/com/dayliane/
├── ai/
│   ├── AiController.java          # AI 用户端接口
│   ├── AiService.java             # AI 调用封装
│   └── AdminAiController.java     # AI 管理接口
```

---

## 五、验收标准

### 分组与排序
- 用户可创建、重命名、删除自定义分组
- 分组支持上移/下移排序
- 任务可拖拽排序，支持跨分组拖拽
- 右键菜单各功能正常

### AI 功能
- 自然语言「明天上午 9 点开会」正确解析为日程草稿
- AI 拆解「完成 Q3 报告」返回合理子任务
- 首页 AI 计划建议基于当前任务状态生成
- AI 优化按钮可润色描述文字
- 用户关闭 AI 记录后不再保存 input/output
- 后台 AI 配置、调用记录、测试功能正常

### 主题与动效
- 日间/夜间主题切换正常，刷新后保持
- 动画流畅，不影响操作效率

### 浏览器通知
- 授权后收到通知时弹出浏览器通知
- 点击通知可跳转到对应页面

### 构建与测试
- [x] `src/backend`：`mvn test`，10 个验收测试通过
- [x] `src/web-user`：`npm test`、`npm run typecheck`、`npm run build` 通过
- [x] `src/admin-web`：`npm test`、`npm run build` 通过
- [x] 桌面与 375px 手机视口无横向溢出，关键页面无控制台错误
