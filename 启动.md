1. 启动后端 Spring Boot

推荐使用项目根目录的 Java 17 启动入口（不会改变系统默认 Java，也不会影响其他 Java 8 项目）：

```powershell
cd "D:\trae\dayliane"
.\start-backend-java17.cmd
```

也可以手动进入后端目录后启动：

```powershell
cd "d:\trae\dayliane\src\backend"
.\mvnw.cmd spring-boot:run
```


2. 启动用户端 web-user

cd "d:\trae\dayliane\src\web-user"
npm install
npm run dev


3. 启动管理后台 admin-web

cd "d:\trae\dayliane\src\admin-web"
npm install
npm run dev

