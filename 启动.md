1. 启动后端 Spring Boot

推荐使用项目根目录的 Java 17 启动入口（不会改变系统默认 Java，也不会影响其他 Java 8 项目）：

```powershell
cd "D:\trae\dayliane"
.\start-backend-java17.cmd
```

也可以手动进入后端目录后启动：

```powershell
cd "d:\trae\dayliane\src\backend"
.\mvnw.cmd spring-boot:run
```


2. 启动用户端 web-user

cd "d:\trae\dayliane\src\web-user"
npm install
npm run dev


3. 启动管理后台 admin-web

cd "d:\trae\dayliane\src\admin-web"
npm install
npm run dev


4. 启用腾讯云邮箱验证码

项目已经接好邮箱注册、验证码校验和腾讯云 SES API。首次启用时，把以下配置加入启动后端的环境变量（不要把 SecretId/SecretKey 写进前端或提交到 Git）：

```powershell
$env:TENCENT_SES_ENABLED = "true"
$env:TENCENT_SES_REGION = "ap-hongkong"
$env:TENCENT_SES_SECRET_ID = "你的腾讯云SecretId"
$env:TENCENT_SES_SECRET_KEY = "你的腾讯云SecretKey"
$env:TENCENT_SES_FROM_EMAIL = "dayline@dayline.art"
$env:TENCENT_SES_TEMPLATE_ID = "213067"
$env:EMAIL_REGISTRATION_ENABLED = "true"
$env:EMAIL_OTP_SECRET = "请替换为一段独立的随机长字符串"
```

然后在同一个 PowerShell 窗口启动后端（项目脚本会自动切换到 Java 17）：

```powershell
cd "D:\trae\dayliane"
.\start-backend-java17.cmd
```

开发页面的注册流程是：填写 QQ 邮箱 → 点击“获取验证码” → 输入验证码、密码 → 点击“创建账号”。

如果使用 Docker，把根目录 `.env.docker.example` 复制为 `.env`，填写 `TENCENT_SES_SECRET_ID` 和 `TENCENT_SES_SECRET_KEY`，并将 `TENCENT_SES_ENABLED` 改为 `true`，再执行 `docker compose up --build -d`。

