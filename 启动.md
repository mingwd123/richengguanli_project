# Dayliane 启动指南

> 本机实测核对时间：2026-09-13。所有命令均在 Windows PowerShell 下验证过。

## 0. 启动顺序与端口约定

按顺序启动：**MySQL → 后端(8080) → 用户端(5173) / 管理后台(5174) → 桌面端（可选）**

| 服务 | 访问地址 | 实际绑定 | 说明 |
| --- | --- | --- | --- |
| 后端 Spring Boot | http://127.0.0.1:8080 | 0.0.0.0 | API 前缀 `/api/v1`，健康检查 `/actuator/health` |
| 用户端 web-user | http://127.0.0.1:5173 | 127.0.0.1 | 仅本机可访问 |
| 管理后台 admin-web | http://127.0.0.1:5174 | 0.0.0.0 | `vite.config.js` 里 `host: true`，局域网内可访问 |
| 数据库 MySQL | 127.0.0.1:3306 | 0.0.0.0 | 库名 `dayline` |

前后端不依赖 Vite 代理：用户端和管理后台都直接请求 `http://127.0.0.1:8080/api/v1`（见 `src/*/src/api/http.ts` 的 `VITE_API_BASE_URL` 兜底值）。**后端必须跑在 8080，否则两个前端页面都会请求失败。**

## 1. 前置条件

- **JDK 17**：本机位于 `D:\JDK(java)`（17.0.3.1）。注意系统默认 Java 是 JDK 8，详见第 5 节。
- **Node.js + npm**：本机为 Node 24（`D:\Node_24`），已验证可用。
- **MySQL 8.0+**：本机服务名 `MySQL80`（自动启动 / Running），开发库 `dayline`，默认账号密码 `root` / `mysql`（`application-dev.yml` 的兜底值）。若连接失败，先确认服务是否在跑。
- 桌面端额外需要 Rust/Cargo 和 Tauri 的 Windows 构建工具。

## 2. 启动后端（推荐，也是唯一稳妥的方式）

```powershell
cd "D:\trae\dayliane"
.\start-backend-java17.cmd
```

脚本 `start-backend-java17.cmd` 会依次做三件事：

1. **自动读取根目录 `.env`**（若存在）并逐行注入为本窗口的环境变量 —— 邮箱验证码等配置不需要每次手动 `set`。
2. 把 `JAVA_HOME` 指向 `D:\JDK(java)` 并前置到 `PATH`，**不会改变系统默认 Java 8**，也不影响其他 Java 8 项目。
3. `cd` 到 `src\backend` 执行 `mvnw.cmd spring-boot:run`。

启动成功后默认地址 `http://localhost:8080`，用下面命令确认：

```powershell
curl.exe http://127.0.0.1:8080/actuator/health
# 期望输出：{"status":"UP"}
```

如果本机 JDK 17 不在 `D:\JDK(java)`，先修改 `start-backend-java17.cmd` 里的 `JAVA_HOME`。

带参数运行 Maven 时可以直接透传，例如：

```powershell
.\start-backend-java17.cmd test
```

## 3. 启动用户端 web-user

```powershell
cd "D:\trae\dayliane\src\web-user"
npm install          # 只在 package.json 有变动时执行一次
npm run dev
```

访问 http://127.0.0.1:5173

## 4. 启动管理后台 admin-web

```powershell
cd "D:\trae\dayliane\src\admin-web"
npm install
npm run dev
```

访问 http://127.0.0.1:5174

## 5. 为什么不能直接跑 `.\mvnw.cmd spring-boot:run`

本机系统 `JAVA_HOME` 指向 **JDK 8**（`D:\jdk-8u461-windows-x64\jdk1.8.0_461`），而项目是 Spring Boot 3.4.5 + `java.version=17`。直接用 `mvnw.cmd` 会用 JDK 8 去加载 Spring Boot 插件，报错：

```
[WARNING] Error injecting: org.springframework.boot.maven.RunMojo
java.lang.TypeNotPresentException: Type org.springframework.boot.maven.RunMojo not present
```

**项目本身没有问题，是执行命令的 Java 版本不对。**

如果确实需要直接用 `mvnw.cmd`，在同一个窗口里先临时切到 JDK 17（只对当前窗口生效）：

```powershell
cd "D:\trae\dayliane\src\backend"
$env:JAVA_HOME = "D:\JDK(java)"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
.\mvnw.cmd spring-boot:run
```

日常开发建议统一用第 2 节的 `start-backend-java17.cmd`，省掉这一步。

## 6. 启动桌面端（可选）

桌面端在 `src/desktop`，需要 Rust/Cargo 环境：

```powershell
cd "D:\trae\dayliane\src\desktop"
npm install
npm run tauri:dev
```

只想在浏览器里预览桌面布局（不需要 Rust）：

```powershell
npm run dev -- --port 1420
```

## 7. 邮箱验证码（腾讯云 SES）

根目录 `.env` 已经被 `start-backend-java17.cmd` 自动加载，**正常情况下不需要再手动设环境变量**。相关变量：

| 变量 | 作用 |
| --- | --- |
| `TENCENT_SES_ENABLED` | 是否启用腾讯云 SES 发信 |
| `TENCENT_SES_REGION` | 地域，当前 `ap-hongkong` |
| `TENCENT_SES_SECRET_ID` / `TENCENT_SES_SECRET_KEY` | 腾讯云密钥 |
| `TENCENT_SES_FROM_EMAIL` | 发信地址 `dayline@dayline.art` |
| `TENCENT_SES_TEMPLATE_ID` | 模板 ID |
| `EMAIL_REGISTRATION_ENABLED` | 是否开放邮箱注册 |
| `EMAIL_OTP_SECRET` | 验证码签名密钥，必须是独立随机长字符串 |

注意事项：

- `.env` 里是**真实密钥**，已被 `.gitignore` 忽略，不要提交、不要复制进任何文档或聊天记录。
- 若改用命令行临时注入，请照抄 `.env` 里已有的真实值，**不要填占位文字**（例如把 `TENCENT_SES_SECRET_ID` 设成「你的腾讯云SecretId」），否则后端能启动但发信会静默失败。
- 开发页面的注册流程：填写 QQ 邮箱 → 点击「获取验证码」 → 输入验证码、密码 → 点击「创建账号」。

## 8. Docker 部署与本地开发不要同时开

`docker-compose.yml` 会占用宿主机的 **8080、5173、5174**，与本地开发的端口完全重叠。

- 本地开发时请确认 Docker Desktop 没有在跑这套 compose。
- 需要 Docker 方式运行时，把根目录 `.env.docker.example` 复制为 `.env` 并填好密钥，再执行 `docker compose up --build -d`。

## 9. 启动失败排查清单

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `TypeNotPresentException: ... RunMojo not present` | 用了系统默认 JDK 8 跑 `mvnw.cmd` | 用 `start-backend-java17.cmd`，或按第 5 节临时切 JDK 17 |
| `Port 8080 was already in use` | 残留的 Java 进程或 Docker 容器占着端口 | `netstat -ano \| findstr 8080` 找到 PID 后 `taskkill /PID <pid> /F` |
| 后端起来了但页面数据加载失败 | 后端没跑在 8080（前端直连 8080） | 确认 `/actuator/health` 返回 `{"status":"UP"}` |
| `Communications link failure` / 连接被拒绝 | MySQL80 服务没启动 | 启动 `MySQL80` 服务后重试 |
| 前端 `npm run dev` 报 5173/5174 被占用 | 已有旧 dev server 在跑 | 关掉旧窗口，或加 `--port` 换端口 |
| 管理后台端口从局域网可访问 | `admin-web/vite.config.js` 里 `host: true` | 如需收紧，改成 `host: '127.0.0.1'` |
| 发验证码失败但服务不报错 | `.env` 里的腾讯云密钥是占位值或已轮换 | 更新 `.env` 里的真实密钥 |

## 10. 本地测试账号

统一密码 `Abc12345`，登录接口字段是 **`account`**（不是 `username`）：

| 账号 | 角色 |
| --- | --- |
| 13800138000 | 团队所有者 |
| 13900139000 | 成员 |
| 13900139001 | 管理员 |
| 13900139002 / 13900139003 | 成员 |

> 仅供本地开发和功能测试使用，请勿用于生产环境。
