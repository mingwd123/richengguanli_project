# V1.2 开发计划：计划完善与协作体验增强版

基于 MVP 与 V1.1 已实现能力，V1.2 主要补齐计划文档中尚未完整落地的功能，并继续优化日历、团队任务、筛选、AI 确认流程和后台审计。

---

## 一、版本目标

V1.2 的目标不是新增大量独立功能，而是把现有产品从“功能可用”推进到“流程完整、展示清晰、管理可追踪”。

重点方向：
- 完善日历页的当天任务查看能力。
- 完善团队任务详情中的操作记录时间轴。
- 统一个人日程、团队任务、后台列表的筛选与排序体验。
- 补齐分组折叠、逾期置顶、倒计时悬停详情等 DeskHive 风格交互。
- 标准化 AI 生成草稿后的确认流程。
- 补齐 AI 团队任务拆解和描述优化在创建/编辑场景中的入口。
- 补全后台重要操作审计日志。

---

## 二、开发阶段

### 阶段 1：日历页增强

#### 用户端
- [ ] 日历页点击某一天后，展示当天任务详情列表。
- [ ] 当天任务列表同时包含个人日程和分配给我的团队任务。
- [ ] 右侧增加当天时间轴，按时间从早到晚排列。
- [ ] 时间轴节点展示任务类型、标题、所属模块、截止时间、剩余时间或逾期时间。
- [ ] 点击当天任务项跳转对应详情：
  - 个人日程跳转 `/schedules/:id`
  - 团队任务跳转 `/tasks/:id`
- [ ] 修复跨时区日期匹配问题，保证上海时区下日期数量和详情一致。

#### 验收标准
- 点击日历任意日期，可以看到该日期下的个人日程和团队任务。
- 日历数量、当天列表、当天时间轴三者数据一致。
- 时间轴中的团队任务和个人日程都可以正确跳转详情页。

---

### 阶段 2：团队任务操作时间轴

#### 后端
- [ ] 设计团队任务操作记录来源。
- [ ] 支持展示以下节点：
  - 创建任务
  - 分配任务
  - 接受任务
  - 拒绝任务
  - 完成任务
  - 重新分配
  - 取消任务
  - 恢复任务
  - 修改时间
  - 修正执行人状态
- [ ] 若现有表无法完整还原操作历史，新增或扩展操作记录表。
- [ ] 提供团队任务详情接口中的 `timeline` 字段，或新增独立接口查询操作时间轴。

#### 用户端
- [ ] 团队任务详情页新增操作时间轴区域。
- [ ] 时间轴按操作时间倒序或正序展示。
- [ ] 每个节点显示操作者、操作类型、时间和关键说明。
- [ ] 重新分配节点显示原执行人和新执行人。

#### 验收标准
- 团队任务详情页能清楚看到任务从创建到当前状态的关键变化。
- 接受、拒绝、完成、重新分配、取消等操作后，时间轴立即刷新。

---

### 阶段 3：统一列表筛选与排序

#### 后端
- [ ] 统一列表接口参数：
  - `page`
  - `size`
  - `keyword`
  - `status`
  - `date_from`
  - `date_to`
  - `sort`
- [ ] 个人日程列表补齐关键词、日期范围、排序能力。
- [ ] 团队任务列表补齐关键词、状态、日期范围、排序能力。
- [ ] 管理后台用户、团队、日程、团队任务、通知、提醒列表统一分页和筛选规则。

#### 用户端
- [ ] 个人日程页增加关键词搜索。
- [ ] 个人日程页增加状态筛选和日期范围筛选。
- [ ] 团队任务页增加关键词搜索、状态筛选和日期范围筛选。
- [ ] 筛选条件变化后自动刷新列表。
- [ ] 保持分页、筛选、排序参数和页面状态一致。

#### 管理后台
- [ ] 用户管理支持关键词和状态筛选。
- [ ] 团队管理支持关键词筛选。
- [ ] 日程管理支持用户、状态、日期范围筛选。
- [ ] 团队任务管理支持团队、状态、日期范围筛选。
- [ ] 通知记录和提醒记录支持状态、日期范围筛选。

#### 验收标准
- 用户端和后台主要列表都能按关键词、状态、日期范围筛选。
- 列表分页和筛选条件切换稳定，不出现数据错乱。

---

### 阶段 4：分组与逾期体验完善

#### 用户端
- [ ] 首页、个人日程列表、团队任务列表统一分组折叠样式。
- [ ] 分组标题显示：展开/收起箭头、分组名称、未完成数量。
- [ ] 已完成任务独立进入“已完成”分组。
- [ ] 逾期未完成任务在对应分组内置顶。
- [ ] 倒计时标签增加悬停详情：
  - 创建时间
  - 开始时间
  - 截止时间
  - 剩余时间
  - 提醒时间
- [ ] 已完成任务显示“已完成”，已取消任务显示“已取消”，不再显示倒计时。

#### 验收标准
- 每个分组都可以折叠和展开。
- 逾期任务在分组顶部显示，样式明确但不过度干扰。
- 鼠标悬停倒计时标签可以看到完整时间信息。

---

### 阶段 5：AI 草稿确认流程

#### 用户端
- [ ] AI 自然语言解析日程后，先展示“AI 草稿确认”状态。
- [ ] 用户可以在草稿中修改标题、描述、模块、时间类型、时间字段和提醒时间。
- [ ] 用户点击确认后，才调用普通日程创建接口保存。
- [ ] 草稿确认弹窗明确提示：AI 不会直接写入数据库。
- [ ] 解析失败时保留用户输入，方便重新尝试。

#### 后端
- [ ] 保持 AI 接口只返回草稿，不直接创建数据。
- [ ] AI 日志记录继续遵守用户的记录开关。

#### 验收标准
- AI 解析不会自动创建日程。
- 用户确认前可以完整修改草稿。
- 用户确认保存后，首页、日程列表、日历和时间轴同步刷新。

---

### 阶段 6：AI 团队任务增强

#### 用户端
- [ ] 创建团队任务页接入 AI 拆解入口。
- [ ] 输入团队目标后，AI 返回多个子任务草稿。
- [ ] 用户可以勾选需要创建的子任务。
- [ ] 用户可以逐条修改子任务标题、描述、截止时间和执行人。
- [ ] 确认后批量创建团队任务。
- [ ] 团队任务创建/编辑描述输入框增加 AI 优化按钮。

#### 后端
- [ ] 保持 `/api/v1/ai/team-tasks/breakdown` 返回子任务建议，不直接写库。
- [ ] 保持 `/api/v1/ai/text/optimize-task-description` 返回优化后的描述建议。

#### 验收标准
- AI 拆解结果可以被用户选择和编辑。
- 确认后才真正创建团队任务。
- AI 优化描述可以应用到创建和编辑流程。

---

### 阶段 7：后台审计补全

#### 后端
- [ ] 梳理所有后台重要操作。
- [ ] 补齐 `admin_operation_log` 记录范围：
  - 修改用户状态
  - 修改管理员账号状态
  - 创建管理员账号
  - 修改团队任务状态
  - 修改执行人状态
  - 修改 AI 配置
  - 启用/关闭 AI
- [ ] 日志记录操作人、操作类型、对象类型、对象 ID、修改前数据、修改后数据、IP 和 User Agent。

#### 管理后台
- [ ] 操作日志页面支持关键词、操作类型、对象类型、日期范围筛选。
- [ ] 日志详情支持查看修改前后数据。

#### 验收标准
- 后台关键修改操作都能在操作日志中追踪。
- 管理员可以按操作类型和时间范围查询日志。

---

### 阶段 8：右键菜单与拖拽排序完善

#### 用户端
- [ ] 任务卡片右键菜单统一支持：
  - 编辑任务
  - 修改截止时间
  - 移除截止时间
  - 移动到分组
  - 删除任务
- [ ] 分组标题右键菜单统一支持：
  - 重命名分组
  - 删除分组
  - 上移
  - 下移
  - 折叠/展开
- [ ] 支持分组内任务拖拽排序。
- [ ] 支持跨分组拖拽任务，并自动更新分组。
- [ ] 支持已完成任务独立分组内排序。
- [ ] 拖拽过程提供低干扰视觉反馈。

#### 后端
- [ ] 继续复用现有 `sort_order` 和排序接口。
- [ ] 确认个人日程和团队任务跨分组移动后排序值稳定。

#### 验收标准
- 右键菜单功能完整可用。
- 拖拽排序刷新后仍保持。
- 跨分组拖拽后任务所属分组正确更新。

---

## 三、新增/重点页面清单

### 用户端

| 页面/组件 | 说明 | 状态 |
|-----------|------|------|
| 日历当日详情面板 | 点击日期后展示当天个人日程和团队任务 | 待完成 |
| 日历当日时间轴 | 展示当天任务时间顺序 | 待完成 |
| 团队任务操作时间轴 | 展示创建、分配、接受、拒绝、完成、重新分配、取消等记录 | 待完成 |
| AI 草稿确认弹窗 | AI 解析后先确认再保存 | 待完成 |
| AI 团队任务拆解确认区 | 选择和编辑 AI 拆解出的子任务 | 待完成 |
| 倒计时悬停详情 | 展示创建/开始/截止/提醒/剩余时间 | 待完成 |
| 统一右键菜单 | 任务卡片与分组标题右键操作 | 待完善 |
| 拖拽排序交互 | 组内排序、跨组移动、已完成分组排序 | 待完善 |

### 管理后台

| 页面/组件 | 说明 | 状态 |
|-----------|------|------|
| 操作日志详情 | 查看修改前后数据 | 待完善 |
| 后台列表筛选 | 用户、团队、日程、任务、通知、提醒列表统一筛选 | 待完善 |
| 团队成员管理增强 | 更清晰地查看和管理团队成员 | 待完善 |

---

## 四、接口增强清单

### 用户端接口

- [ ] `GET /api/v1/schedules` 补齐 keyword/date_from/date_to/sort。
- [ ] `GET /api/v1/team-tasks/my` 补齐 keyword/date_from/date_to/sort。
- [ ] `GET /api/v1/teams/{teamId}/tasks` 补齐 keyword/date_from/date_to/sort。
- [ ] `GET /api/v1/reminders/my` 查询我的提醒记录。
- [ ] 团队任务详情接口增加操作时间轴字段，或新增独立时间轴接口。

### 管理后台接口

- [ ] 后台用户、团队、日程、团队任务、通知、提醒列表统一筛选参数。
- [ ] 操作日志列表支持操作类型、对象类型、关键词和日期范围筛选。
- [ ] 所有重要后台修改接口写入操作日志。

---

## 五、验收标准

### 日历增强
- 点击日期可以看到当天所有任务。
- 日历数量、详情列表和当日时间轴数据一致。
- 个人日程和团队任务都能正确跳转详情。

### 团队任务时间轴
- 团队任务详情页能展示完整操作轨迹。
- 接受、拒绝、完成、重新分配、取消等操作后时间轴刷新正确。

### 筛选与排序
- 用户端和后台主要列表支持关键词、状态、日期范围、排序。
- 筛选与分页切换稳定。

### 分组与逾期体验
- 分组可以折叠展开。
- 逾期任务置顶。
- 倒计时悬停展示详细时间。

### AI 流程
- AI 只生成草稿或建议，不直接写库。
- 用户确认后才保存。
- AI 拆解团队任务和描述优化在创建/编辑流程可用。

### 后台审计
- 后台关键修改操作均写入操作日志。
- 操作日志可查询、可查看详情。

### 构建与测试
- `src/backend`：`mvnw test` 通过。
- `src/web-user`：`npm run build` 通过。
- `src/admin-web`：`npm run build` 通过。
