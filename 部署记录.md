# Dayliane 服务器部署记录

> 最后更新：2026-08-19（Asia/Shanghai）  
> 项目：`mingwd123/richengguanli_project`，分支：`main`  
> 服务器部署目录：`/opt/dayliane`  
> 记录性质：实际操作记录 + 后续维护手册

## 1. 结论先看

### 当前已完成

- Dayliane 的四个 Docker 服务已经在同一台腾讯云服务器运行：`mysql`、`backend`、`web-user`、`admin-web`。
- MySQL 通过 Docker 持久化卷保存数据，当前卷名为 `dayliane_dayliane_mysql`。
- 后端、用户端和管理端容器均设置为 `restart=unless-stopped`。
- 容器端口只绑定到服务器本机回环地址：
  - `127.0.0.1:8080 -> backend:8080`
  - `127.0.0.1:5173 -> web-user:80`
  - `127.0.0.1:5174 -> admin-web:80`
- MySQL 没有映射宿主机 `3306`，只在 Docker 网络内提供服务。
- 宿主机 Nginx 已配置为公网 IP 的 HTTP 入口，80 端口反向代理到用户网页。
- UFW 已启用，服务器本机只放行 `22/tcp`、`80/tcp`、`443/tcp`。
- `/opt/dayliane` 已改为 Git 浅克隆，可以用 `git pull --ff-only origin main` 更新。
- 旧的非 Git 部署目录仍保留为 `/opt/dayliane-before-git-20260819`，用于紧急回滚参考。

### 尚不能称为“完整生产化”的部分

- 当前没有域名、HTTPS 和 ICP 备案；现在的公网 IP 访问是 HTTP，只适合开发/测试阶段。
- 腾讯云安全组还需要在控制台最终核验是否确实只有 22、80、443。
- 正式管理员账号尚未完成安全初始化。
- 数据库异地自动备份尚未完成。
- 尚未完成服务器重启后的自动恢复演练。
- AI 仍按要求关闭：`AI_ENABLED=false`。

因此，准确表述是：**基础应用部署已经完成并可用，生产化收尾仍有待办。**

## 目录

- [1. 结论先看](#1-结论先看)
- [2. 部署前已完成的基础条件](#2-部署前已完成的基础条件)
- [3. 服务器、项目与安全边界](#3-服务器项目与安全边界)
- [4. 阶段一：确认目录并准备源码](#4-阶段一确认目录并准备源码)
- [5. 阶段二：创建生产环境 `.env`](#5-阶段二创建生产环境-env)
- [6. 阶段三：端口隔离与首次启动](#6-阶段三端口隔离与首次启动)
- [7. 阶段四：宿主机 Nginx 与 IP 访问](#7-阶段四宿主机-nginx-与-ip-访问)
- [8. 阶段五：UFW 与公网端口](#8-阶段五ufw-与公网端口)
- [9. 阶段六：从源码包迁移到可 `git pull` 的正式目录](#9-阶段六从源码包迁移到可-git-pull-的正式目录)
- [10. 账号、数据和访问现状](#10-账号数据和访问现状)
- [11. 以后版本更新流程](#11-以后版本更新流程当前正式流程)
- [12. 回滚原则](#12-回滚原则)
- [13. 数据库备份](#13-数据库备份重要待办)
- [14. 已遇到的问题、原因和解决方案](#14-已遇到的问题原因和解决方案)
- [15. 当前检查结果清单](#15-当前检查结果清单)
- [16. 日常维护检查清单](#16-日常维护检查清单)
- [17. 尚未完成事项与建议顺序](#17-尚未完成事项与建议顺序)
- [18. 敏感信息处理声明](#18-敏感信息处理声明)

---

## 2. 部署前已完成的基础条件

以下条件在本次实际部署前已经准备好；这里记录验证方式，便于以后排查环境问题：

```bash
docker --version
```

```bash
docker compose version
```

```bash
docker run --rm hello-world
```

作用：分别确认 Docker、Compose 版本以及 Docker 容器运行链路。`hello-world` 测试成功，说明 Docker 守护进程、镜像拉取和普通用户运行权限基本可用。

另外，服务器已配置腾讯云 Docker 镜像加速，`ubuntu` 用户已加入 `docker` 组；后续仍可能因为旧 SSH 会话没有重新加载组权限而需要执行 `newgrp docker`，详见第 6 节。

---

## 3. 服务器、项目与安全边界

### 服务器环境

| 项目 | 实际情况 |
|---|---|
| 云厂商 | 腾讯云，中国大陆地域 |
| 操作系统 | Ubuntu 24.04.4 LTS |
| 架构 | x86_64 |
| CPU/内存 | 4 核，约 4GB 内存 |
| 系统盘 | 40GB，部署时约剩余 33GB |
| Swap | 约 2GB |
| 公网带宽 | 约 3Mbps |
| SSH 用户 | `ubuntu` |
| 应用目录 | `/opt/dayliane` |
| Docker | 29.1.3 |
| Docker Compose | 2.40.3 |

### 部署拓扑

```text
公网 IP:80
    │
    ▼
宿主机 Nginx
    │ 反向代理
    ▼
127.0.0.1:5173 ──> web-user 容器 :80

127.0.0.1:5174 ──> admin-web 容器 :80（只允许服务器本机，未公网暴露）
127.0.0.1:8080 ──> backend 容器 :8080（只允许服务器本机）

backend ── Docker 网络 ──> mysql:3306 ──> Docker 卷 dayliane_dayliane_mysql
```

公网安全目标：只开放 `22`、`80`、`443`；不开放 `3306`、`8080`、`5173`、`5174`。

### 不可违反的安全红线

1. 不在聊天、截图或 Git 中发送/保存真实的 `DB_PASSWORD`、`JWT_SECRET`、`REMINDER_SCAN_TOKEN`、`AI_API_KEY_ENCRYPTION_SECRET`、AI API Key、私钥或账号密码。
2. 不执行 `docker compose down -v`。该命令会删除 Compose 管理的持久化卷，可能导致数据库数据丢失。
3. 修改配置前先备份，尤其是数据库和服务器专用 Compose 覆盖文件。
4. 公网 IP + HTTP 不适合承载真实敏感数据；启用正式用户前应完成 HTTPS。

---

## 4. 阶段一：确认目录并准备源码

### 4.1 检查目标目录

```bash
sudo test -e /opt/dayliane && echo EXISTS || echo NOT_EXISTS
```

作用：确认正式部署目录是否已经存在，避免误覆盖已有文件。

实际结果：`NOT_EXISTS`。

```bash
sudo install -d -o ubuntu -g ubuntu -m0755 /opt/dayliane
```

作用：创建部署目录，并把目录所有者设置为 `ubuntu`，便于后续以普通用户运行 Git 和 Compose。

### 4.2 第一次 HTTPS 克隆失败

最初尝试：

```bash
git clone --depth 1 --branch main --single-branch \
  https://github.com/mingwd123/richengguanli_project.git /opt/dayliane
```

作用：通过 GitHub HTTPS 获取 `main` 分支的浅克隆。

现象：长时间停在 `Cloning into ...`，SSH 连接中断。

中断后检查：

```bash
pgrep -af '^git clone( |$)' || echo "NO_GIT_CLONE_PROCESS"
```

作用：确认连接中断后是否还有残留的 Git 克隆进程。

实际结果：`NO_GIT_CLONE_PROCESS`。

```bash
ls -la /opt/dayliane
```

作用：确认中断是否留下半成品。

实际结果：目录中只有 `.` 和 `..`，没有残留源码，可以安全重试。

### 4.3 网络诊断与临时源码包方案

```bash
curl -I --connect-timeout 10 --max-time 30 https://github.com
```

作用：只测试服务器到 GitHub 网页入口的 HTTPS 连通性，不下载项目。

```bash
timeout 30 git ls-remote \
  https://github.com/mingwd123/richengguanli_project.git HEAD
```

作用：测试 Git 智能 HTTPS 接口是否可用；`timeout` 返回 `124` 表示超时，不是 Git 仓库损坏。

```bash
curl -I -L --connect-timeout 10 --max-time 30 \
  https://codeload.github.com/mingwd123/richengguanli_project/tar.gz/refs/heads/main
```

作用：测试 GitHub 的源码打包下载入口。该入口可用时，可先用源码包完成首次部署。

```bash
curl -fL --retry 3 --connect-timeout 10 --max-time 180 \
  https://codeload.github.com/mingwd123/richengguanli_project/tar.gz/refs/heads/main \
  -o /tmp/dayliane-main.tar.gz
```

作用：下载 `main` 分支源码压缩包到临时目录；`--retry 3` 用于短暂网络波动时自动重试。

```bash
tar -tzf /tmp/dayliane-main.tar.gz | head
```

作用：先查看压缩包目录结构，确认顶层目录名和文件确实存在。

```bash
tar -xzf /tmp/dayliane-main.tar.gz --strip-components=1 -C /opt/dayliane
```

作用：解压源码，并去掉压缩包的顶层目录，使文件直接落到 `/opt/dayliane`。

重要结果：源码包部署没有 `.git` 历史，因此当时的 `/opt/dayliane` 不能直接执行 `git pull`。这也是后来进行 Git 迁移的原因。

---

## 5. 阶段二：创建生产环境 `.env`

### 5.1 为什么必须先创建 `.env`

Compose 文件使用了必填变量语法，例如：

```yaml
DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
JWT_SECRET: ${JWT_SECRET:?Set JWT_SECRET in .env}
REMINDER_SCAN_TOKEN: ${REMINDER_SCAN_TOKEN:?Set REMINDER_SCAN_TOKEN in .env}
```

所以必须先创建并填写 `/opt/dayliane/.env`，再执行：

```bash
docker compose config --quiet
```

如果在 `.env` 尚未创建时执行该命令，Compose 报缺少 `DB_PASSWORD`、`JWT_SECRET` 等变量是**预期现象**，不代表项目配置损坏。

### 5.2 模板检查

```bash
test -f .env.docker.example && echo TEMPLATE_PRESENT || echo TEMPLATE_MISSING
```

作用：确认当前源码目录是否带有 Docker 环境变量模板。

实际部署源码包中检查结果为 `TEMPLATE_MISSING`。当时仓库的忽略规则包含：

```text
.env.*
!.env.example
```

因此不能假定服务器下载的源码包一定带有 `.env.docker.example`。当前工作区若能看到模板，也应以服务器实际文件为准。

### 5.3 生成秘密并写入 `.env`（示例，不含真实值）

以下是**仅限首次创建且目标 `.env` 不存在时**的安全写法。它只在服务器本地生成随机值并写文件，不把秘密打印到终端；如果 `.env` 已经存在，必须停止，不要重新生成，否则会导致现有数据库密码、JWT 和已加密 AI Key 失效。实际生产值不应复制到聊天或文档中。

```bash
cd /opt/dayliane
if [ -e .env ]; then
  echo ".env already exists; stop and preserve it"
  exit 1
fi
umask 077
DB_PASSWORD="$(openssl rand -hex 32)"
JWT_SECRET="$(openssl rand -hex 64)"
REMINDER_SCAN_TOKEN="$(openssl rand -hex 32)"
AI_API_KEY_ENCRYPTION_SECRET="$(openssl rand -hex 32)"
{
  printf 'DB_NAME=dayline\n'
  printf 'DB_PASSWORD=%s\n' "$DB_PASSWORD"
  printf 'JWT_SECRET=%s\n' "$JWT_SECRET"
  printf 'REMINDER_SCAN_TOKEN=%s\n' "$REMINDER_SCAN_TOKEN"
  printf 'DEMO_DATA_ENABLED=false\n'
  printf 'AI_ENABLED=false\n'
  printf 'AI_PROVIDER=deepseek\n'
  printf 'AI_MODEL=deepseek-chat\n'
  printf 'AI_API_BASE_URL=https://api.deepseek.com/v1\n'
  printf 'AI_API_KEY=\n'
  printf 'AI_API_KEY_ENCRYPTION_SECRET=%s\n' "$AI_API_KEY_ENCRYPTION_SECRET"
  printf 'AI_ALLOWED_HOSTS=api.deepseek.com\n'
} > .env
unset DB_PASSWORD JWT_SECRET REMINDER_SCAN_TOKEN AI_API_KEY_ENCRYPTION_SECRET
chmod 600 .env
```

变量作用：

| 变量 | 作用 | 本次状态 |
|---|---|---|
| `DB_PASSWORD` | MySQL root 密码 | 已生成，真实值不记录 |
| `JWT_SECRET` | Access/Refresh Token 签名密钥 | 已生成，真实值不记录 |
| `REMINDER_SCAN_TOKEN` | 内部提醒扫描接口令牌 | 已生成，真实值不记录 |
| `AI_API_KEY_ENCRYPTION_SECRET` | 加密数据库中 AI Key 的密钥 | 已生成，真实值不记录 |
| `DEMO_DATA_ENABLED` | 是否导入演示数据 | `false` |
| `AI_ENABLED` | 是否启用 AI | `false` |

权限检查：

```bash
stat -c '%a %U:%G %n' .env
```

实际结果：`600 ubuntu:ubuntu /opt/dayliane/.env`。

只输出变量是否存在，不输出值的检查方式：

```bash
for v in DB_PASSWORD JWT_SECRET REMINDER_SCAN_TOKEN AI_API_KEY_ENCRYPTION_SECRET; do
  grep -qE "^${v}=[^[:space:]]+$" .env && echo "$v=SET" || echo "$v=MISSING"
done
grep -E '^(DEMO_DATA_ENABLED|AI_ENABLED)=' .env
```

最后再校验 Compose：

```bash
docker compose config --quiet
```

实际结果：无输出、退出码为 0，表示 Compose 配置语法和必填变量均通过。

---

## 6. 阶段三：端口隔离与首次启动

### 6.1 备份原 Compose 文件

```bash
cp -p docker-compose.yml /tmp/dayliane-docker-compose.yml.before-local-bind
```

作用：修改端口绑定前保留原文件，便于比较或恢复。

### 6.2 将应用端口绑定到本机

首次源码包部署阶段，把默认端口改成：

```text
127.0.0.1:8080:8080
127.0.0.1:5173:80
127.0.0.1:5174:80
```

作用：Docker 端口只在服务器本机监听，公网流量统一经 Nginx 进入；这样不会直接暴露后端、用户前端或管理前端。

修改后检查：

```bash
grep -nE '127\.0\.0\.1:(8080|5173|5174)' docker-compose.yml
```

```bash
docker compose config --quiet
```

```bash
ss -ltn | grep -E ':(3306|8080|5173|5174)([[:space:]]|$)' || echo TARGET_PORTS_FREE
```

作用：分别确认端口配置、Compose 语法和启动前端口是否空闲。

### 6.3 Docker socket 权限问题

第一次启动：

```bash
docker compose up --build -d
```

作用：构建 `backend`、`web-user`、`admin-web` 镜像，并以后台方式启动全部服务。

第一次结果：出现类似以下权限错误：

```text
permission denied ... /var/run/docker.sock
```

原因：`ubuntu` 已加入 `docker` 组，但当前 SSH 会话还没有加载新的组权限。

检查：

```bash
id
getent group docker
```

修复当前会话：

```bash
newgrp docker
```

作用：启动一个使用新组权限的当前 shell。之后再次执行 `id`，应看到 `docker` 组，并且：

```bash
docker ps
```

不再出现 socket 权限错误。

然后重新启动：

```bash
docker compose up --build -d
```

实际结果：四个服务成功启动，MySQL 健康检查通过，并创建 Docker 网络和持久化卷。

### 6.4 容器和端口验证

```bash
docker compose ps
```

最终应看到四个服务处于 `Up`，MySQL 为 `Up (healthy)`，端口类似：

```text
dayliane-mysql-1       Up (healthy)   3306/tcp, 33060/tcp
dayliane-backend-1     Up              127.0.0.1:8080->8080/tcp
dayliane-web-user-1    Up              127.0.0.1:5173->80/tcp
dayliane-admin-web-1   Up              127.0.0.1:5174->80/tcp
```

注意：`3306/tcp` 出现在容器信息中不等于公网开放；本次没有宿主机 `3306` 映射。

健康检查：

```bash
curl -fsS --max-time 10 http://127.0.0.1:8080/actuator/health
```

实际结果：`{"status":"UP"}`。

用户端页面：

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' http://127.0.0.1:5173/
```

实际结果：`HTTP 200`。

管理端页面：

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' http://127.0.0.1:5174/
```

用户确认管理后台访问正常；该次没有保留具体的命令输出。预期结果为 `HTTP 200`。

未登录 API：

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' \
  http://127.0.0.1:5173/api/v1/user/profile
```

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' \
  http://127.0.0.1:5174/api/v1/admin/profile
```

实际结果：均为 `HTTP 401`。这表示 API 链路和鉴权生效，不是服务故障。

---

## 7. 阶段四：宿主机 Nginx 与 IP 访问

### 7.1 安装和检查 Nginx

```bash
nginx -v
```

实际结果：最初提示 `command not found`，说明尚未安装。

```bash
sudo apt update
```

作用：刷新 Ubuntu 软件包索引。

```bash
sudo apt install -y nginx
```

作用：安装宿主机 Nginx。安装后 Nginx 1.24 系列服务启动。

安装过程提示系统存在待加载的新内核；这不是 Dayliane 启动错误，重启安排到维护窗口处理。

```bash
systemctl is-active nginx
```

实际结果：`active`。

### 7.2 停用默认站点并保留备份

```bash
sudo cp -a /etc/nginx/sites-available/default \
  /etc/nginx/sites-available/default.before-dayliane
```

作用：备份 Ubuntu 默认站点配置。

```bash
sudo unlink /etc/nginx/sites-enabled/default
```

作用：停用默认站点的启用链接；没有删除原配置文件。

### 7.3 创建 IP 入口配置

文件：`/etc/nginx/sites-available/dayliane-ip.conf`

核心配置如下（实际公网 IP 不写入记录）：

```nginx
server {
    listen 80 default_server;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:5173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/dayliane-ip.conf \
  /etc/nginx/sites-enabled/dayliane-ip.conf
```

检查并平滑重载：

```bash
sudo nginx -t
```

```bash
sudo systemctl reload nginx
```

实际结果：`syntax is ok`、`test is successful`，重载完成。

入口验证：

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' http://127.0.0.1/
```

实际结果：`HTTP 200`。

通过 Nginx 的 API 验证：

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' \
  http://127.0.0.1/api/v1/user/profile
```

实际结果：`HTTP 401`，说明 Nginx 到后端的代理链路正常，401 来自鉴权层。

当前管理后台仍通过 `127.0.0.1:5174` 访问，不直接公开到公网。没有域名时不要擅自配置 HTTPS；443 只是防火墙预留端口，当前没有 HTTPS 监听。

---

## 8. 阶段五：UFW 与公网端口

初始检查：

```bash
sudo ufw status verbose
```

实际结果：最初为 `inactive`。

先放行 SSH，再准备 Web 端口：

```bash
sudo ufw allow 22/tcp
```

```bash
sudo ufw allow 80/tcp
```

```bash
sudo ufw allow 443/tcp
```

作用：在启用防火墙前保留 SSH、HTTP、HTTPS 入口，避免把自己锁在服务器外。

设置默认策略：

```bash
sudo ufw default deny incoming
```

```bash
sudo ufw default allow outgoing
```

最后启用：

```bash
sudo ufw enable
```

最终检查：

```bash
sudo ufw status verbose
```

实际状态：

```text
Status: active
Default: deny (incoming), allow (outgoing)
允许：22/tcp、80/tcp、443/tcp（IPv4 与 IPv6）
```

监听检查：

```bash
sudo ss -ltnp
```

确认结果：

- 公网监听：SSH 22、Nginx 80。
- 本机监听：Docker 代理的 8080、5173、5174。
- 宿主机没有 3306。
- 443 尚未有 Nginx HTTPS 监听。

腾讯云安全组是另一层防线，必须在云控制台再次确认只放行 22、80、443；UFW 不能替代安全组。

部署过程中看到的 `atkbd serio0: Unknown key pressed` 与 `UFW BLOCK ... DST=224.0.0.1` 属于系统键盘/组播日志，与 Dayliane 无关。

---

## 9. 阶段六：从源码包迁移到可 `git pull` 的正式目录

### 9.1 为什么需要迁移

源码压缩包只有文件，没有 `.git` 目录，所以：

```bash
git -C /opt/dayliane pull --ff-only origin main
```

在最初部署目录不可用。不能通过在生产目录随意 `git init` 来“伪造”历史，否则容易覆盖或混淆当前部署状态。

### 9.2 选择 GitHub SSH over 443

普通 Git HTTPS 智能协议曾超时，因此使用 GitHub SSH 的 443 端口。

可达性检查：

```bash
timeout 15 ssh-keyscan -p 443 -t ed25519 ssh.github.com >/dev/null && \
  echo SSH_443_REACHABLE || echo SSH_443_UNAVAILABLE
```

检查专用密钥路径是否空闲：

```bash
if [ -e "$HOME/.ssh/dayliane_github" ] || [ -e "$HOME/.ssh/dayliane_github.pub" ]; then
  echo KEY_PATH_EXISTS
else
  echo KEY_PATH_FREE
fi
```

生成专用 Ed25519 密钥：

```bash
ssh-keygen -t ed25519 \
  -f "$HOME/.ssh/dayliane_github" \
  -C dayliane-production-readonly \
  -N ''
```

作用：为本项目创建独立的 GitHub Deploy Key。公钥只授予仓库只读权限，不勾选 `Allow write`。

注意：私钥路径可以记录，私钥内容和 `.pub` 文件全文不能写入本文档。

核对 GitHub SSH 主机指纹：

```bash
ssh-keyscan -p 443 -t ed25519 ssh.github.com 2>/dev/null | \
  ssh-keygen -lf - -E sha256
```

本次核对到 GitHub 官方 Ed25519 指纹：

```text
SHA256:+DiY3wvvV6TuJJhbpZisF/zLDA0zPMSvHdkr4UvCOqU
```

写入已核对的主机指纹：

```bash
ssh-keyscan -p 443 -t ed25519 ssh.github.com 2>/dev/null >> \
  "$HOME/.ssh/known_hosts"
chmod 600 "$HOME/.ssh/known_hosts"
```

测试 GitHub 认证：

```bash
ssh -T -p 443 \
  -i "$HOME/.ssh/dayliane_github" \
  -o IdentitiesOnly=yes \
  git@ssh.github.com
```

实际结果：

```text
You've successfully authenticated, but GitHub does not provide shell access.
```

这表示认证成功；GitHub 不提供 shell 登录是正常提示。

### 9.3 临时克隆并迁移生产配置

```bash
cd /opt
if [ -e /opt/dayliane-git ]; then echo BACKUP_TARGET_EXISTS; else echo BACKUP_TARGET_FREE; fi
```

```bash
sudo install -d -o ubuntu -g ubuntu -m0755 /opt/dayliane-git
```

使用 SSH 443 克隆浅历史：

```bash
GIT_SSH_COMMAND='ssh -i /home/ubuntu/.ssh/dayliane_github -o IdentitiesOnly=yes' \
git clone --depth 1 --branch main --single-branch \
  ssh://git@ssh.github.com:443/mingwd123/richengguanli_project.git \
  /opt/dayliane-git
```

实际结果：克隆成功，约 6.18 MiB，374 个对象。

让该仓库以后默认使用专用密钥：

```bash
git -C /opt/dayliane-git config core.sshCommand \
  'ssh -i /home/ubuntu/.ssh/dayliane_github -o IdentitiesOnly=yes'
```

验证拉取：

```bash
git -C /opt/dayliane-git pull --ff-only origin main
```

实际结果：`Already up to date.`。

复制已有生产 `.env`，不重新生成数据库密码：

```bash
install -m600 /opt/dayliane/.env /opt/dayliane-git/.env
```

为了避免把旧 `.env` 中的任何值打印出来，迁移时可用哈希比较“除 Compose 两项外是否保持一致”（实际结果为 `ENV_PRESERVED`）：

```bash
old_env_hash="$(grep -vE '^(COMPOSE_FILE|COMPOSE_PROJECT_NAME)=' /opt/dayliane/.env | sha256sum | cut -d' ' -f1)"
new_env_hash="$(grep -vE '^(COMPOSE_FILE|COMPOSE_PROJECT_NAME)=' /opt/dayliane-git/.env | sha256sum | cut -d' ' -f1)"
[ "$old_env_hash" = "$new_env_hash" ] && echo ENV_PRESERVED || echo ENV_CHANGED
unset old_env_hash new_env_hash
```

### 9.4 服务器专用 Compose 覆盖文件

为了让未来的 `git pull` 不会把本机端口安全设置冲掉，在 `/opt/dayliane-git/docker-compose.server.yml` 使用未提交的服务器覆盖配置：

实际通过编辑器创建该文件（重现时可执行 `nano /opt/dayliane-git/docker-compose.server.yml`）；下面的 YAML 是**文件内容**，不是要直接粘贴到 Bash 中执行的命令。

```yaml
services:
  mysql:
    restart: unless-stopped

  backend:
    restart: unless-stopped
    ports: !override
      - "127.0.0.1:8080:8080"

  web-user:
    restart: unless-stopped
    ports: !override
      - "127.0.0.1:5173:80"

  admin-web:
    restart: unless-stopped
    ports: !override
      - "127.0.0.1:5174:80"
```

在 `.env` 追加以下两项（值本身不敏感）：

```text
COMPOSE_FILE=docker-compose.yml:docker-compose.server.yml
COMPOSE_PROJECT_NAME=dayliane
```

若需要用命令追加，先确认同名变量不存在，避免重复追加或覆盖已有配置（未执行的可复用写法）：

```bash
grep -q '^COMPOSE_FILE=' .env || printf '%s\n' 'COMPOSE_FILE=docker-compose.yml:docker-compose.server.yml' >> .env
grep -q '^COMPOSE_PROJECT_NAME=' .env || printf '%s\n' 'COMPOSE_PROJECT_NAME=dayliane' >> .env
chmod 600 .env
```

作用：

- `COMPOSE_FILE` 让每次 Compose 操作自动加载服务器安全覆盖文件。
- `COMPOSE_PROJECT_NAME=dayliane` 保持项目名稳定，避免在不同目录误创建第二套容器或第二个数据库卷。

把服务器覆盖文件加入本地排除而不是提交到 GitHub：

```bash
grep -qxF 'docker-compose.server.yml' .git/info/exclude || \
  printf '%s\n' 'docker-compose.server.yml' >> .git/info/exclude
```

校验最终合并配置：

```bash
docker compose config --quiet
```

查看关键服务的重启策略和端口（只打印结构信息，不打印 `.env` 值）：

```bash
docker compose config --format json | python3 -c \
'import json,sys; c=json.load(sys.stdin); print("\n".join("{}: restart={}, ports={}".format(n,c["services"][n].get("restart","none"),",".join("{}:{}->{}".format(p.get("host_ip","*"),p["published"],p["target"]) for p in c["services"][n].get("ports",[])) or "none") for n in ("mysql","backend","web-user","admin-web")))'
```

预期关键结果：

```text
mysql: restart=unless-stopped, ports=none
backend: restart=unless-stopped, ports=127.0.0.1:8080->8080
web-user: restart=unless-stopped, ports=127.0.0.1:5173->80
admin-web: restart=unless-stopped, ports=127.0.0.1:5174->80
```

检查 Git 工作树：

```bash
git status --short --branch
```

实际结果：`## main...origin/main`，工作树干净。

### 9.5 切换正式目录

先确认备份名称没有被占用：

```bash
cd /opt
if [ -e /opt/dayliane-before-git-20260819 ]; then
  echo BACKUP_TARGET_EXISTS
else
  echo BACKUP_TARGET_FREE
fi
```

切换目录（保留旧目录，不删除）：

```bash
sudo mv /opt/dayliane /opt/dayliane-before-git-20260819
```

```bash
sudo mv /opt/dayliane-git /opt/dayliane
```

验证新正式目录：

```bash
git -C /opt/dayliane status --short --branch
```

```bash
git -C /opt/dayliane pull --ff-only origin main
```

实际结果：分支为 `main`，工作树干净，拉取结果为 `Already up to date.`。

应用服务器覆盖配置并复用原数据库卷：

```bash
cd /opt/dayliane
docker compose up -d
```

说明：这里没有使用 `down -v`，也没有删除数据库卷；Compose 根据稳定项目名识别并复用了原有容器/卷。

最终状态检查：

```bash
docker compose ps
```

```bash
docker inspect -f '{{.Name}} restart={{.HostConfig.RestartPolicy.Name}}' \
  dayliane-mysql-1 dayliane-backend-1 dayliane-web-user-1 dayliane-admin-web-1
```

实际结果：四个容器均为 `restart=unless-stopped`，MySQL 为 `healthy`，端口仍只绑定 `127.0.0.1`。

---

## 10. 账号、数据和访问现状

### 10.1 演示账号不存在是预期结果

生产配置固定为：

```text
DEMO_DATA_ENABLED=false
```

因此不会导入固定演示用户或演示管理员。网页可以正常打开和登录入口；普通用户应通过产品提供的注册流程创建账号。

这不是数据库损坏，也不是容器启动失败。正式管理员账号尚未完成初始化，不能把仓库中的测试账号密码直接当作生产账号使用。

### 10.2 当前访问方式

- 用户端：`http://服务器公网IP/`（经 Nginx 80 端口）。
- 管理端：服务器本机 `http://127.0.0.1:5174/`；如需从个人电脑访问，应使用 SSH 隧道，不要把 5174 改成公网监听。
- 后端健康检查：服务器本机 `http://127.0.0.1:8080/actuator/health`。

当前为 HTTP 明文访问。域名、DNS、HTTPS 和 ICP 备案按用户要求暂缓，项目开发完成后再做。

---

## 11. 以后版本更新流程（当前正式流程）

`/opt/dayliane` 现在是 Git 浅克隆，因此不需要每次重新下载源码压缩包。正常更新只拉取 Git 中的跟踪文件；`.env` 和 `docker-compose.server.yml` 不会被 `git pull` 覆盖。

建议一次只执行一个命令，并在每一步确认结果：

### 11.1 更新前检查

```bash
cd /opt/dayliane
```

```bash
git status --short --branch
```

作用：确认当前目录和分支正确、没有未提交的服务器本地修改。预期类似 `## main...origin/main`。

如果这里出现未知修改，先停止更新并核对，不要直接覆盖。

### 11.2 拉取代码

```bash
git pull --ff-only origin main
```

作用：只接受可以快进的更新，避免在生产目录自动产生合并提交。没有新版本时输出 `Already up to date.`。

### 11.3 校验合并后的 Compose

```bash
docker compose config --quiet
```

作用：在重建容器前检查 YAML、变量和服务器覆盖配置。若失败，先修复配置，不要启动。

### 11.4 构建并重建需要更新的服务

涉及后端或前端代码时：

```bash
docker compose up --build -d
```

作用：重新构建镜像，并重新创建需要更新的容器；Compose 不保证严格的“滚动更新”，更新期间可能有短暂中断。MySQL 数据卷不会因该命令被删除。

只改了非镜像运行配置时，可使用：

```bash
docker compose up -d
```

### 11.5 更新后验证

```bash
docker compose ps
```

```bash
curl -fsS --max-time 10 http://127.0.0.1:8080/actuator/health
```

```bash
curl -sS --max-time 10 -o /dev/null -w 'HTTP %{http_code}\n' http://127.0.0.1/
```

必要时查看最近日志：

```bash
docker compose logs --tail=100 backend
```

```bash
docker compose logs --tail=100 web-user admin-web
```

### 11.6 更新流程注意事项

- 如果版本包含数据库迁移，更新前先做一次数据库备份，并记录迁移说明。
- 不要执行 `docker compose down -v`。
- 不要在 `/opt/dayliane-before-git-20260819` 目录直接运行 Compose，除非是在明确的回滚演练中，并且已核对 `.env`、项目名和端口。
- 当前是 `--depth 1` 浅克隆，只保留最近历史。需要查看完整历史或回滚较早版本时，先执行（未执行）：

  ```bash
  git fetch --unshallow origin
  ```

---

## 12. 回滚原则

### 12.1 Git 版本回滚（建议流程，未在本次执行）

1. 先备份数据库。
2. 当前仓库是浅克隆；如果目标提交不在最近历史中，先补齐历史：

   ```bash
   git fetch --unshallow origin
   ```

3. 查看可用提交：

   ```bash
   git log --oneline --decorate -n 10
   ```

4. 确认目标提交后，再以维护窗口执行版本切换；不要在未确认提交的情况下使用破坏性命令。
5. 重新运行：

   ```bash
   docker compose config --quiet
   ```

   ```bash
   docker compose up --build -d
   ```

6. 用 `docker compose ps`、健康检查和网页访问验证。

### 12.2 旧目录回滚参考

旧部署目录：

```text
/opt/dayliane-before-git-20260819
```

该目录目前保留，未删除。它是源码包部署快照，不是自动回滚按钮；其中可能包含生产 `.env`，应按敏感数据保护，限制目录权限，不得上传或发给他人。建议先检查（未执行）：

```bash
sudo stat -c '%a %U:%G %n' /opt/dayliane-before-git-20260819 /opt/dayliane-before-git-20260819/.env
```

如权限过宽，再在确认路径无误后收紧（未执行）：

```bash
sudo chmod 700 /opt/dayliane-before-git-20260819
sudo chmod 600 /opt/dayliane-before-git-20260819/.env
```

使用前必须先确认：

- 是否仍含正确的 `.env`；
- Compose 项目名是否仍为 `dayliane`；
- 端口是否只绑定 `127.0.0.1`；
- 当前数据库卷和容器是否会被同一项目正确复用。

不要为了回滚删除数据库卷，也不要直接执行 `down -v`。

---

## 13. 数据库备份（重要待办）

本次已确认数据存放在 Docker 卷 `dayliane_dayliane_mysql`，但**异地自动备份尚未完成**。卷存在不等于具备异地容灾能力。

后续建议在服务器本地先生成逻辑备份，再加密传到另一台可信存储；以下仅为待执行模板。它不把密码直接写成 `mysqldump -p密码` 的命令行参数，但容器环境变量仍应按敏感数据保护：

> 以下备份命令在本次部署中未执行；正式启用前应先做一次小范围恢复演练。

```bash
sudo install -d -o ubuntu -g ubuntu -m 700 /opt/dayliane-backups
```

```bash
cd /opt/dayliane
set -eu -o pipefail
umask 077
backup_file="/opt/dayliane-backups/dayline-$(date +%F-%H%M%S).sql.gz"
tmp_file="${backup_file%.gz}.sql"
docker compose exec -T mysql sh -c \
  'MYSQL_PWD="$MYSQL_ROOT_PASSWORD" exec mysqldump -uroot --single-transaction --routines --events "$MYSQL_DATABASE"' \
  > "$tmp_file"
gzip -f "$tmp_file"
test -s "$backup_file"
gzip -t "$backup_file"
```

作用：使用容器内的 `MYSQL_PWD` 传递密码，避免密码出现在 `mysqldump` 参数列表；`set -euo pipefail`、临时文件、`test -s` 和 `gzip -t` 用于避免把失败、空文件或损坏压缩包当成成功备份。`MYSQL_DATABASE` 应与实际数据库名一致（本次为 `dayline`）。

执行后还必须把备份副本发送到异地存储。不要把备份文件、密码或远端凭据提交到 Git。

建议保留：每日备份、至少一份异地副本、定期恢复演练和过期备份清理策略。

---

## 14. 已遇到的问题、原因和解决方案

| 问题 | 原因 | 解决方案 | 最终状态 |
|---|---|---|---|
| HTTPS `git clone` 长时间无响应，连接中断 | GitHub 智能 HTTPS 在该网络路径上超时 | 先用 `codeload.github.com` 源码包完成首次部署，之后改用 SSH over 443 + Deploy Key | 已解决 |
| 中断后担心半成品残留 | 克隆被中断 | `pgrep` 确认无进程，`ls -la /opt/dayliane` 确认目录为空 | 已确认安全重试 |
| `timeout` 返回 `124` | 命令超时 | 将其解释为网络路径超时，不误判为项目损坏 | 已绕过 |
| `.env.docker.example` 找不到 | 部署时下载内容未包含该模板，且 `.env.*` 被忽略 | 手动创建 `.env`，先补齐必填变量再运行 `docker compose config --quiet` | 已解决 |
| 若在 `.env` 前执行 Compose 校验 | Compose 的必填变量保护会报错（本次实际部署未发生） | 先创建 `.env`，再校验；这是预期的变量检查行为 | 预防说明 |
| `docker.sock permission denied` | 当前 SSH 会话未加载 `docker` 组 | 执行 `newgrp docker`，再重试 Compose | 已解决 |
| Compose 端口可能公网暴露 | 默认文件使用了未绑定 IP 的端口写法 | 首次部署改为 `127.0.0.1`；迁移后用 `docker-compose.server.yml` 覆盖 | 已解决 |
| UFW 初始为 inactive | 系统默认未启用防火墙 | 先放行 22/80/443，再设置默认策略并启用 | 已解决 |
| 演示账号不存在 | `DEMO_DATA_ENABLED=false` 是生产安全设置 | 使用注册流程；不要为了找账号而打开演示数据 | 预期行为 |
| 源码包目录无法 `git pull` | 没有 `.git` 历史 | 新建浅克隆目录、复制生产 `.env`、迁移并保留旧目录 | 已解决 |
| 使用 `ECHO` 出现命令错误 | Bash 命令区分大小写，正确命令是 `echo` | 改用小写 `echo` 或 `printf` | 已解决 |
| 看到 `atkbd` / `224.0.0.1` 日志 | 系统键盘/组播背景日志 | 与 Dayliane 无关，不作为应用故障处理 | 已确认无关 |

---

## 15. 当前检查结果清单

| 检查项 | 状态 | 说明 |
|---|---|---|
| Docker/Compose 可用 | PASS | Docker 29.1.3、Compose 2.40.3，`hello-world` 成功 |
| `.env` 存在且权限为 600 | PASS | 真实值未记录 |
| `docker compose config --quiet` | PASS | 必填变量和合并配置通过 |
| MySQL 健康检查 | PASS | `healthy` |
| MySQL 持久化卷 | PASS | `dayliane_dayliane_mysql` |
| 后端健康接口 | PASS | `{"status":"UP"}` |
| 用户端/管理端页面 | PASS | 本机 HTTP 200 |
| 未登录鉴权 | PASS | API HTTP 401 为预期 |
| 应用端口隔离 | PASS | 8080/5173/5174 仅 `127.0.0.1` |
| 宿主机无 3306 | PASS | 未映射到公网或宿主机 |
| Nginx IP 入口 | PASS | 80 端口 HTTP 200 |
| UFW | PASS | 入站默认拒绝，只允许 22/80/443 |
| Git 正式目录 | PASS | `/opt/dayliane` 可 `git pull --ff-only` |
| GitHub Deploy Key | PASS | SSH 443 认证成功，只读 |
| 自动重启策略 | PASS | 四个容器 `unless-stopped` |
| 域名/HTTPS/ICP | PENDING | 按计划暂缓 |
| 腾讯云安全组复核 | PENDING | 需在控制台完成 |
| 正式管理员初始化 | PENDING | 尚未完成 |
| 数据库异地备份 | PENDING | 尚未完成 |
| 重启恢复演练 | PENDING | 尚未完成 |
| 日志/监控/磁盘告警 | PENDING | 尚未完成 |

---

## 16. 日常维护检查清单

每次更新或排查时按顺序执行，每次执行一个命令并确认结果：

```bash
cd /opt/dayliane
```

```bash
git status --short --branch
```

```bash
docker compose config --quiet
```

```bash
docker compose ps
```

```bash
curl -fsS --max-time 10 http://127.0.0.1:8080/actuator/health
```

```bash
df -h / /opt
```

```bash
sudo ufw status verbose
```

```bash
sudo ss -ltnp
```

检查重点：

- MySQL 必须为 `healthy`。
- 8080、5173、5174 必须仍是 `127.0.0.1` 绑定。
- 不应出现宿主机 3306 监听。
- `.env` 权限保持 600。
- 不要把日志中的 Token、密码或请求头复制到聊天/工单。

---

## 17. 尚未完成事项与建议顺序

1. **数据库异地备份**：先做可恢复的逻辑备份，再配置异地上传和定期恢复演练。
2. **正式管理员账号**：通过项目支持的安全流程初始化，不使用仓库测试账号密码。
3. **腾讯云安全组复核**：入站只保留 22、80、443，确认没有 3306、8080、5173、5174。
4. **重启恢复测试**：安排维护窗口，验证 Docker、Compose 容器、Nginx、UFW 是否自动恢复；同时处理待加载内核。
5. **域名、HTTPS、ICP 备案**：项目开发完成后再实施；中国大陆服务器公开域名访问需要先完成 ICP 备案。
6. **日志与监控**：增加 Docker 日志轮转、磁盘空间告警、容器健康监控。
7. **浅克隆历史**：需要长期回滚能力时执行 `git fetch --unshallow origin`，并评估磁盘占用。
8. **临时文件清理**：`/tmp/dayliane-main.tar.gz` 可能仍存在；确认不再需要后再单独删除，不能误删生产目录或数据库卷。

---

## 18. 敏感信息处理声明

本文档有意不包含以下任何真实内容：

- `.env` 的实际值；
- `DB_PASSWORD`、`JWT_SECRET`、`REMINDER_SCAN_TOKEN`、`AI_API_KEY_ENCRYPTION_SECRET`；
- `AI_API_KEY` 或其他服务商密钥；
- `/home/ubuntu/.ssh/dayliane_github` 私钥内容；
- `dayliane_github.pub` 公钥全文；
- 普通用户、管理员或测试账号密码。

文档只记录变量名、用途、路径、权限和验证方式。任何需要共享的诊断输出都应先脱敏，尤其是环境变量、Authorization 头、Cookie、内部 Token 和数据库连接字符串。
